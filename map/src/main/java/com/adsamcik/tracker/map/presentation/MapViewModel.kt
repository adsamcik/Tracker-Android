package com.adsamcik.tracker.map.presentation

import androidx.lifecycle.ViewModel

/** Minimal ViewModel for map functionality (v2-only simplified). */
class MapViewModel : ViewModel() {
    // Simplified for v2-only architecture
    val state: String = "active" // Placeholder state
}
