<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />


    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!--
        Runtime types follow active sources: LOCATION only while GPS collection is active,
        HEALTH while activity/step collection is active, and SPECIAL_USE only for a genuinely
        uncovered signal-only session. Permission possession alone never adds a type.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <service
            android:name="com.adsamcik.tracker.tracker.service.TrackerService"
            android:exported="false"
            android:foregroundServiceType="location|health|specialUse"
            tools:targetApi="q">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-visible tracking session collection of pressure, Wi-Fi scan, and cell-radio context when no dedicated foreground-service type applies" />
        </service>

        <activity
            android:name="com.adsamcik.tracker.tracker.shortcut.ShortcutActivity" />

        <receiver
            android:name="com.adsamcik.tracker.tracker.receiver.TrackerNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.adsamcik.tracker.tracker.receiver.TrackerTimeUnlockReceiver"
            android:exported="false"/>

    </application>
</manifest>
