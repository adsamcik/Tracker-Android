# Tracking Infrastructure Verification Matrix

Last updated: 2026-09-01

Status meanings: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `IN_REVIEW`, `DONE`. `DONE` requires production repository evidence, not provider registration alone.

## Static source × mode matrix

| ID | Source | Mode | Capture sources | Declared controls | Qualified `RECORDING` evidence | Canonical output | Production history/UI assertion | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TI-V001 | Location | Manual | Location | none | Fresh eligible post-start point | Protected current location contract | correct day/session or coverage state | `IN_REVIEW` |
| TI-V002 | Location | Automatic | Location | Activity | fresh control starts; fresh eligible point records | protected current location contract | correct day/session; no captured control Activity | `IN_REVIEW` |
| TI-V003 | Wi-Fi | Manual | Wi-Fi | none | fresh post-start callback/result; a finite direct-demand scan budget may seek first evidence | privacy-safe snapshot header plus Wi-Fi day facts | day count/band mix, identities concealed, no Location capture source | `BLOCKED` |
| TI-V004 | Wi-Fi | Automatic | Wi-Fi | Activity | fresh control starts; fresh Wi-Fi callback records | privacy-safe Wi-Fi day facts | correct day/session; no captured control Activity | `BLOCKED` |
| TI-V005 | Cell | Manual | Cell | none | fresh post-start callback; a finite direct-demand refresh budget may seek first evidence | minimized snapshot header plus quality/technology facts | day quality/technology, no tower identity or Location capture source | `BLOCKED` |
| TI-V006 | Cell | Automatic | Cell | Activity | fresh control starts; fresh Cell callback records | minimized quality/technology facts | correct day/session; no captured control Activity | `BLOCKED` |
| TI-V007 | Activity | Manual | Activity | none | fresh captured classification above threshold | coalesced movement bands | day active time/movement | `BLOCKED` |
| TI-V008 | Activity | Automatic | Activity | Activity control purpose on shared registration | fresh control starts; capture-consented trigger handoff seeds the first band or the interval stays partial until fresh captured evidence | coalesced movement bands with explicit handoff provenance | one physical registration, purpose-safe history | `BLOCKED` |
| TI-V009 | Steps | Manual | Steps | none | positive post-baseline delta | canonical day/session step delta | total and session partition | `IN_REVIEW` |
| TI-V010 | Steps | Automatic | Steps | Activity Transition; optional Step corroboration only if separately enabled | fresh legal control starts; positive post-baseline delta records | canonical day/session step delta | no hidden/captured control facts | `BLOCKED` |
| TI-V011 | Pressure | Manual | Pressure | none | first committed qualified microsegment/window under capture generation | pressure trend/window plus coverage | correct session/day, gap and stability state | `BLOCKED` |
| TI-V012 | Pressure | Automatic | Pressure | Activity Transition | fresh legal control starts; first qualified pressure window records | pressure trend/window plus coverage | correct day/session; no captured control Activity | `BLOCKED` |

No TI-V001–TI-V012 row is `DONE`: production reads remain split and largely trip/`SessionSegment`
driven. A Hilt-bound `TrackingHistoryRepository` feeds selected Trip Detail, exact live Steps-only
presentation, and one bounded stopped Dashboard recent-history list. Qualified stopped Steps-only
entries now have ordinary Dashboard discovery, but day/Calendar/Statistics composition, exact
capture/Location evidence, safe deletion, and cross-surface product-query proof remain absent.

Review verdicts are source lifecycle/product verdicts rather than work-item states: Location and
Steps are `DEGRADED`; Wi-Fi, Cell, Activity, and Pressure are `FAILED`. Location remains `IN_REVIEW`
until its contradicting canonical-owner metadata is dispositioned. Steps manual remains `IN_REVIEW`
because its candidate writer/read/fence path is dormant, active legacy deletion is unresolved, and
bounded host queryability does not prove the exact manual provider/listener or terminal contract.
Wi-Fi, Cell, Activity, and Pressure remain
`BLOCKED` on terminal product paths; the Wi-Fi clean-install permission mismatch is locally
corrected but still requires device proof.

## Scope-proportionality gate

The three-review scope round did not authorize weakening the source contracts above. TI-D054 makes sole-source capture explicit: each manual only-X path captures only X, and each automatic only-X path captures only X plus exactly its declared controls. Under TI-D055/TI-D056, every valid source-qualified unit under current physical/authorization generations has a durable representation; control units remain bounded operational evidence and absent from product history unless independently product-eligible. The release-sized evidence model is shared model/property tests for cross-source invariants, one parameterized source-adapter contract, six manual production-query slices, one shared automatic-control contract exercised against six capture plans, exact v27→v28 migration/recovery execution, and a small representative physical-provider smoke. Broader OEM/Doze/multi-SIM/soak coverage is risk-based backlog rather than fabricated fleet evidence.

| ID | Scope assertion | Status | Evidence / exit condition |
| --- | --- | --- | --- |
| TI-VS01 | Every retained v28 table has a next-milestone production owner | `IN_REVIEW` | the 12 ownerless generic tables and duplicate lifecycle lease were removed; v28 now has 69 entities: all 51 released-v27 entities plus 18 narrowly owned additions, including two v27 recovery records, Activity automatic-action/epoch state, source-local product-lane activation/cursor/retention state, immutable selected-session history, and the dormant source deletion fence. Historical 66-entity evidence remains valid only for its earlier checkpoint. Per-table retention/export/deletion ownership and a production deletion-fence producer remain incomplete. |
| TI-VS02 | Upgrade preserves or explicitly re-consents automatic tracking | `IN_REVIEW` | focused policy tests prove enabled legacy automatic mode grants Activity `CONTROL` only and mode transitions append grant/revoke epochs without changing capture. The current implementation marks that control nonpersistent; TI-D055 requires durable purpose-limited admission. Connected populated migration passes; automatic trigger and no-control-capture product/device proof remain. |
| TI-VS03 | Database merge import excludes v28 control-plane rows | `PASS_LOCAL` | production merge uses a 24-table user-fact allowlist; the hostile-backup test proves an Activity fact imports while foreign policy authority and ACTIVE demand rows do not. Device restore coverage and derived recomputation remain separate gates. |
| TI-VS04 | Exact Android migration boundary is executed, not compile-only | `IN_REVIEW` | seven populated v27→v28 containment cases pass on `Medium_Phone`, including WAL sequence, retained-floor, activation-floor, outbox-only, and pure-outbox high-watermark boundaries. The focused frozen runtime drain and process-wide startup/deletion ordering pass host tests at `f14a4a2b1`. Connected migrate→startup→drain, production backup-wrapper, portable export/import and cold restore remain blocking before schema freeze. |
| TI-VS05 | One thin Steps path proves the minimum materialization shape | `BLOCKED` | the dormant source-specific fact lane and immutable service-run history selector prove qualified delta identity, cursor/replay fencing, correction/reattribution isolation, deletion tombstones, retained-floor truth and exact active/retired writer selection. `f9b1c3c45` consumes the Hilt-bound selected-session query; TI-B171–TI-B176 add exact reverse binding, capture/qualification authority, logical grouping, a finite candidate-owned page, and exact live presentation. TI-B177–TI-B178 make a qualified stopped Steps-only logical entry ordinarily discoverable and boundedly `QUERYABLE` on Dashboard without `sampleCount`, numeric cross-run totals, or action identity. `1bb9749af` adds a compiling disposable instrumentation harness for the exact provider→WAL→candidate writer→repository→retirement chain, but no device is attached and it has not executed. Still required: the real manual provider/WAL/materialization/listener-removal gate, exact no-other-demand device evidence, rendered UI truth, safe deletion, completeness-safe day/Calendar/Statistics and numeric consumers, portable typed round trip, and an explicit ordinary activation action. No generic materializer or day platform was added. |
| TI-VS06 | Local evidence plan contains only runnable gates | `IN_REVIEW` | retain host contracts, local diagnostics, exact migration/recovery, and representative device smoke; remote cohort/p95/SLO claims remain excluded without an approved evidence channel. |
| TI-VS07 | Full DayOverview/M3 journal is not a source-safety prerequisite | `IN_REVIEW` | first prove TI-D074 availability/evidence/product/coverage axes through existing Dashboard/Calendar/History with no fabricated zero; the full Days-first product remains separately unapproved. |
| TI-VS08 | R2 lean-state candidate reproduces required recovery/fencing | `PASS_LOCAL` | 8/8 pure-model scenarios pass. The production disposition keeps queryable intent/broker rows and unifies only the duplicate lease; focused production coordinator/lease tests pass 21/21. Device process/reboot execution remains separate. |
| TI-VS09 | Ambient-only Steps is visible without a trip | `BLOCKED` | provider/WAL/typed-fact production graph must yield one outside-session day through Today, Timeline, Calendar, selected-day detail, export and deletion with truthful completeness. |
| TI-VS10 | Payload bytes are authenticated before use | `PASS_LOCAL` | a valid-but-mutated payload is rejected before decode, terminally quarantined without projector invocation, and a healthy later ordinal applies. Future typed serialized facts must preserve the same rule. |
| TI-VS11 | Ambient source deletion cannot resurrect from WAL/import/delayed callback | `BLOCKED` | source/purpose/time fence, fact/WAL/cache removal, applicable key rotation and current-generation admission must be atomic/recoverable. |
| TI-VS12 | Source-native durability is bounded and quality-preserving | `BLOCKED` | slow-Room saturation for Location batches, radio snapshots, Activity deliveries, Steps boundaries and 20 Hz Pressure input proves no unreported qualified-unit loss, bounded memory, explicit gaps, and transaction counts proportional to admission units/windows. |
| TI-VS13 | Authorization changes do not restart unchanged providers | `BLOCKED` | repeated manifest, consent-purpose, motion and unrelated-source changes preserve physical registration/config and Step/Pressure state; revocation fences immediately; replacement failure has deterministic completeness. |
| TI-VS14 | Adaptive power control respects direct quality floors | `BLOCKED` | exhaustive policy × demand × constraint property tests plus sole-source device tests prove Pressure never disappears, manual responsive Location is not silently made passive, Activity capture never becomes Transition-only when classifications are required, and Wi-Fi capture never becomes cache-only when fresh callback/result evidence is required. |
| TI-VS15 | Projection work is per-source, asynchronous and poison-bounded | `BLOCKED` | corrupt/retryable Wi-Fi cannot stop Steps/Location cursors; callback latency and projection query/write counts remain bounded over 10,000 mixed events. |
| TI-VS16 | Radio observation/result/no-delivery states remain distinct | `BLOCKED` | fresh empty, unchanged, stale cached, throttled attempt, no callback, direct start probe and multi-SIM cases yield distinct typed header/completeness/product states. |
| TI-VS17 | Existing product consumers preserve source-only output and quality | `BLOCKED` | Today, Timeline, Calendar, selected-day/session detail, radio map, export and deletion reproduce each only-X fact; partial Steps cannot award complete-day goals/streaks; radio heatmap shadow parity passes. |
| TI-VS18 | Each source improves quality/energy or preserves quality at materially lower cost | `BLOCKED` | representative shadow comparisons protect source-specific floors and measure freshness, accuracy, coverage, continuity, completeness, error/outlier behavior and useful context alongside provider events, CPU/wakeups, SQLite transactions/bytes and battery. Reject any candidate that is both lower quality and no more efficient. |
| TI-VS19 | One thin writer owns every tracking data-plane mutation boundary | `BLOCKED` | architecture test/static dependency rule proves WAL/fact/membership/cursor/deletion/import/destination mutations use `TrackingWriter`; policy and lifecycle retain separate atomic control-plane transactions. Cancellation and queue saturation do not advance a cursor or lose a qualified delivery. Instantiate only lanes backed by real source verticals. |
| TI-VS20 | Automatic start is legal and durable before external runtime | `BLOCKED` | API 26–37 tests prove Activity Transition-only cold origins, pinned-client/provider acceptance of the requested transition set (including `ON_FOOT` when present, otherwise explicit WALKING/RUNNING fallback), expiry/current epoch CAS, exact FGS type union, background-Location gate, denial persistence and no service-first window. Sampling callbacks cannot cold-start. |
| TI-VS21 | Process-resilient registrations reconcile without ghost sessions | `BLOCKED` | ordinary process death, boot, app update and force-stop cases prove eligible provider `PendingIntent`s—especially automatic Activity Transitions—are durably desired and re-armed when needed, stale automatic sessions finalize interrupted, and `STOPPING`/`FINALIZED` never restart. |
| TI-VS22 | Ambient Steps continuity has one owner and zero overlap inflation | `BLOCKED` | capability/permission matrix selects exactly one Health Connect or Recording adapter; direct/live overlaps, delayed imports, provider switch, reset, correction and replay preserve one interval total; no-GMS/no-HC falls back truthfully. |
| TI-VS23 | Ambient maintenance is honest and consolidated | `BLOCKED` | one unique WorkManager chain handles import/reconciliation/repair/retention; actual observation/execution times and delay causes are visible; no test or UI claims periodic sampling, including Doze and Android 16 job quota cases. |
| TI-VS24 | v28 migration preserves released facts without fabricating provenance | `IN_REVIEW` | populated Android v27 fixtures retain all six typed families, summaries, radio facts, import state, counter reset and legacy WAL while terminalizing runtime and leaving authority empty. The frozen four-target drain suppresses stale Activity/join effects, preserves Location shadow, bridges Steps/Pressure semantically, labels outbox-only recovery with unknown clock provenance, and quarantines outbox ordinals/effects outside the frozen activation and semantic contract. Process-wide startup integration passes host ordering/deletion tests; connected migrate→drain→query/deletion proof and portable recovery remain blocking. |
| TI-VS25 | Batched deliveries are authorization-homogeneous | `BLOCKED` | for all six sources, delayed evidence crossing enable/revoke, session start/stop, consent epoch and reboot is split/classified by item/subrange observed time; Pressure closes at boundaries and unknowable Step deltas become baseline+gap. |
| TI-VS26 | Typed corrections may change cardinality without residue | `BLOCKED` | one→zero, one→two, out-of-order correction and projector upgrade use stable logical range identity plus explicit upsert/delete/replace-range; final facts/membership/completeness and database hash are identical after replay/crash. |
| TI-VS27 | Scoped deletion survives old import and replay | `BLOCKED` | `03bbda2f1` proves that the dormant candidate Steps lane treats an exact source/purpose/logical-run fence as a validated no-effect and that the selected-session reader invalidates to a named deleted cause. No production producer exists, active legacy Steps and import/retention/future writers do not honor the fence, and no dirty-day recomputation is wired. Still delete each source/purpose/day, stage pre-delete identical/conflicting/legacy-unverifiable imports, and prove no fact or membership returns and derived output equals clean recomputation. |
| TI-VS28 | Legacy/candidate destination switch is atomic | `IN_REVIEW` | merge `9b8ab4b43` adds a contained source-specific transition: it refuses non-idle/finalized runs, requires command/callback/legacy-writer quiescence, atomically promotes lane/owner/rollout authority, contains and drains rollback to an exact cutoff, restores legacy ownership, and re-arms an ABA-safe empty post-deletion generation. Rollout authorization and durable admission recheck exact candidate ownership. TI-B160 connects the production deletion provider, default `AppDatabase`, closed startup/deletion barrier, and actual Steps coordinator through diagnostics failure and retry. `c5118e186` observes historical transition state for one selected session without consulting current ownership, and `f9b1c3c45` renders that state read-only in Trip Detail; no path proves continuous ordinary-navigation/query/export/deletion visibility. Provider/cold-process/device evidence, explicit release action, and an ordinary activation caller remain required. |
| TI-VS29 | Cancellation never consumes retry or advances state | `BLOCKED` | cancel during encode, before/inside admission commit, projector apply, failure recording and post-commit wakeup; cancellation propagates and attempt/cursor/quarantine/facts remain unchanged except a WAL commit proven complete. |
| TI-VS30 | Portable export covers every released source and minimizes radio | `BLOCKED` | six sole-source plus ambient/control fixtures prove all product facts/provenance/completeness export, control-only WAL and raw SSID/BSSID/tower/subscription identity do not, and key rotation does not leak linkability. |
| TI-VS31 | Steps distinguishes baseline, verified zero and positive recording | `BLOCKED` | `dff1b85fd` host-proves only the synthetic integrity-valid capture-attributed WAL boundary: baseline and covered zero do not yield its test-local positive verdict, while a contiguous positive covered window precedes the canonical receipt/cursor. `1bb9749af` compiles a disposable gate that requires a fresh run/manifest/authorization-bound baseline, payload/evidence clock linkage, and only positive covered windows afterward; it has not run on a provider. Production admission/freshness, provider callbacks, restart/reset/replay, rendered accessible device UI, and platform listener removal remain required. |
| TI-VS32 | Product state axes map exhaustively without false zero | `BLOCKED` | six-source consumer tests cover availability × evidence × product/completeness combinations including permission, OS-limit, materializing, partial, degraded and failed; text/TalkBack labels and repair actions are non-color and no null becomes zero. |
| TI-VS33 | Optional Location context is purpose-compatible | `BLOCKED` | same-session capture join succeeds; cross-session, control-only and ambient-primary/capture-only-Location joins fail; consent revoke/deletion removes context while preserving/explaining the primary fact. |
| TI-VS34 | Central owners remain thin under source and maintenance load | `BLOCKED` | architecture rules reject provider/query algorithms in `TrackingWriter`, mutation in history, SQL in supervisor and hardware demand from context/export; slow import/deletion keeps bounded admission and unrelated-source progress. |
| TI-VS35 | Every source switches acquisition modes locally and safely | `BLOCKED` | property tests exercise every adjacent/direct ladder transition and same-source demand merge; unchanged physical keys cause zero provider calls; active→passive Location, Sampling→Transitions Activity, direct→continuity Steps, Pressure rate/FIFO/window, Wi-Fi active-scan budget→passive results and Cell refresh budget→change callbacks preserve eligible facts, record gaps, dedupe overlap, survive failure/revoke/process death and never restart unrelated sources. |
| TI-VS36 | Acquisition rungs have measured mechanism/energy separation | `BLOCKED` | Activity Transition versus Sampling and Pressure rate/FIFO profiles record actual provider requests, FIFO capacity, effective sample rate, batches, callbacks, AP wakeups, CPU and device energy on representative hardware. A rung with no distinct quality/cost behavior collapses; no UI or rollout claim assigns an unmeasured saving. |
| TI-VS37 | Cache, passive callback, and active request semantics cannot converge | `BLOCKED` | Wi-Fi `getScanResults`/broadcast/`startScan` and Cell `getAllCellInfo`/change callback/`requestCellInfoUpdate` fixtures prove cache reads never create new observation time or `RECORDING`, attempts remain operational-only, passive results require platform/capability support, and only fresh returned items become facts. |
| TI-VS38 | Stale or irrelevant payload is neither stored nor used | `BLOCKED` | all six sources cover stale, pre-effective, future, missing-time, mixed-age batch, duplicate, unchanged and fresh-empty inputs. Identity-bearing WAL/facts/export/history/context contain only qualified items; rejected content leaves at most a bounded non-identifying reason counter; compact coverage headers cannot advance a value beyond their observed interval. |
| TI-VS39 | Exact cache replay creates zero durable/product effect | `BLOCKED` | repeated Wi-Fi/Cell cache reads with identical normalized content and per-item provider-time vectors create no WAL row, fact, coverage, `RECORDING`, history/export count or database growth. A later successful callback with advanced qualified provider times but unchanged content creates one compact coverage header referencing prior qualified content; deletion/retention leaves no dangling reference. |
| TI-VS40 | Manual only-X start requests only X's prerequisites | `IN_REVIEW` | 17 source-aware Dashboard decision assertions pass: Pressure, Steps and Activity start with Location permission denied; Location requests Location; Wi-Fi/Cell request fine Location only as their own current platform prerequisite and never enable Location capture. Device permission-result flow, registration-set proof and other manual entry surfaces remain. |
| TI-VS41 | Sessionless ambient day identity is stable | `BLOCKED` | an ambient fact observed near midnight keeps the same structural day across delayed admission/import, process death, Prague↔New York zone changes, and DST gaps/overlaps; clock uncertainty crossing a day boundary produces partial/ambiguous completeness rather than fabricated certainty. |
| TI-VS42 | Metric completeness reaches every decision-making consumer | `BLOCKED` | baseline-only, covered zero, positive partial and complete Steps fixtures flow through Dashboard, Today, Timeline, Calendar, detail, goal/game progress, widget, notification, achievements and streaks; missing/partial data never becomes a complete numeric total or award. |
| TI-VS43 | Source verticals are independently executable | `BLOCKED` | a static plan-DAG test has no cycles; the first Steps lane runs with all other projectors absent; a blocked Ambient Steps capability does not block session Pressure; rollout order remains scheduling preference only. |
| TI-VS44 | One canonical boot clock domain fences policy, registration, and automation | `PASS_LOCAL` | 2026-08-24 focused tests prove readable BOOT_COUNT formatting, process-stable fallback across provider instances, exact registration revocation boundary, and policy effective-time agreement. Reboot/device execution and complete consumer audit remain release evidence. |
| TI-VS45 | Delayed Activity evidence cannot cross an automation-epoch boundary | `PASS_LOCAL` | 2026-08-24 focused delivery/ingress/epoch/outbox/action/finalizer tests prove different-boot and pre-effective observations remain durable facts but cannot acquire the current epoch or actuate automatic start. Process-death/reboot instrumentation remains. |
| TI-VS46 | Acquisition is reachable from a real product lane before rollout | `IN_REVIEW` | rollout schema v3 defaults all sources to `CONTAINED`; missing/v27/old-global rows authorize no provider, and validation rejects `EVENT` without an explicit named-source shadow/canonical stage. Host rollout/session tests pass. No source has yet proven that its declared shadow stage reaches a typed writer and production query, so every real activation remains blocked. |
| TI-VS47 | Pre-WAL provider handoff is bounded and crash-auditable | `BLOCKED` | Location/Wi-Fi overflow, unbounded Cell delivery, failed unregister, storage stall, process death, and generation replacement each preserve the qualified unit or a durable source-local gap; orphan G1/G2 registrations cannot coexist effectively. |
| TI-VS48 | `ACTIVE` and `RECORDING` use different durable evidence | `BLOCKED` | accepted provider/service runtime advances only `ACTIVE`; source-contract-qualified fresh evidence advances `RECORDING`; empty, stale, operational-only, materializing, partial, degraded, and failed cases remain distinguishable through production queries. |
| TI-VS49 | Empty-session reclamation cannot race a final presentation write | `IN_REVIEW` | `88309387d` removes the global periodic database delete, preserves an identity-stamped zero row, and adds positive-sample predicates to daily/live plus source/all-time activity reads; `24b9aeffb` adds them to app-age/hour/night/dawn and ActivityRecognition reads. Every zero-sample row is excluded from those named queries; other DAO reads remain unchanged. UI maintenance requests asynchronous cancellation, deletion awaits it, and an inert wake can occur until completion. `9aeb8853a`/TI-B169 now provide durable exact segment ownership and post-presentation acknowledgement; `f2c7a3225`/TI-B171 requires that reverse binding in selected Steps history. Still required: a typed all-source qualified-fact evaluator, graceful cleanup behind acknowledgement, and later previous-exit/process/device proof. Terminal source state remains insufficient. |

## Ambient policy acceptance matrix

| ID | Source | Current policy result | Allowed acquisition | Required acceptance evidence | Status |
| --- | --- | --- | --- | --- | --- |
| TI-VA01 | Location | `ELIGIBLE`, rollout default off | separately consented passive/opportunistic updates | useful places/coverage product case, setting/consent, minimized retention, export/delete, between-session query, acquisition explanation | `BLOCKED` |
| TI-VA02 | Wi-Fi | `ELIGIBLE`, rollout default off | successful passive scan-result broadcasts; cache retrieval is not acquisition; no default ambient active scans | useful privacy-safe counts/band mix, per-item freshness, keyed minimization, rotation, retention/export/delete, between-session day, truthful OS-limited state | `BLOCKED` |
| TI-VA03 | Cell | `ELIGIBLE`, rollout default off | timestamp-qualified change callbacks; cache reads are bootstrap only; no forced refresh or wake promise | useful technology/quality history, per-item freshness, minimized identity, rotation, retention/export/delete, availability/completeness, multi-SIM behavior | `BLOCKED` |
| TI-VA04 | Activity | `ELIGIBLE`, rollout default off and independent from `CONTROL` | captured ambient Activity only under its own product eligibility; automatic control remains bounded/purpose-limited operational evidence | useful movement bands, independent consent/retention/export/delete and proof that control-only events never enter history | `BLOCKED` |
| TI-VA05 | Steps | `FIRST_VERTICAL`, rollout default off | one selected system continuity adapter (Health Connect device Steps when supported, otherwise Recording API evaluation) plus direct counter only during live direct demand | explicit setting/consent, capability/permission UX, overlap precedence, delayed import, retention/export/delete, ambient-only history/UI, process/reboot/reset behavior | `BLOCKED` |
| TI-VA06 | Pressure | `DISABLED` | no continuous ambient registration | exact demand absence and no between-session Pressure fact | `NOT_STARTED` |

## Required layer assertions applied to every scenario

| Layer | Assertion | Host test evidence | Device evidence |
| --- | --- | --- | --- |
| Policy | Effective manifest equals policy/consent snapshot; disabled capture absent | six-source Room policy tests pass; start/reconfigure append policy-bound manifests and preserve prior revisions | desired-request versus runtime-applicability/failure representation pending |
| Registration | Only requested capture plus declared control/ambient demand active | durable manifest/app demands, physical-only generations and immutable observed-time authorization vectors exist; Activity rotates authorization without provider churn for unchanged GMS configuration; a global reconciler for all six providers remains TI-210 | provider dumps/diagnostics pending |
| Lifecycle | `ACTIVE` follows accepted runtime; `RECORDING` follows qualified durable evidence | source-runtime acceptance, terminal intent, last-source disable, lease ABA/expiry, and stop-cutoff tests pass | Android service/FGS actions, reconciler, process/reboot, and `RECORDING` evidence pending |
| Durability | Crash/retry loses no fact and duplicates no effective contribution | WAL byte authentication and terminal raw quarantine pass; the removed synthetic receipt platform is not evidence for a shipping destination. Typed source-fact identity, recomputation, scoped deletion and kill boundaries remain blocked | kill-at-boundary pending |
| Materialization | One active writer and corrections/deletion are idempotent | no new materializer is active. Each typed destination must prove stable logical identity, one actual writer owner, correction/recompute balance and no resurrection; Location's current writer remains protected | cutover/rollback pending |
| Product | `TrackingHistoryRepository` returns correct historical day, session/outside-session class, source facts, context provenance and completeness to each applicable existing consumer | pending TI-400 | UI/device pending |
| Privacy | control-only facts absent from product history/default export; revoked generations cannot create new facts | revoked capture and stale/pre-effective callbacks are rejected; capture contracts reject control-only attribution; TI-D055 now requires valid control callbacks to remain durable in a bounded operational class rather than be dropped. Scoped retention/deletion/export/HMAC remain blocked | consent/delete/key rotation pending |
| UX | honest state, explanation, repair action | pending TI-500 | TalkBack/large text/reduced motion pending |

## Dynamic and failure coverage

| ID | Scenario family | Required checkpoints | Status | Evidence |
| --- | --- | --- | --- | --- |
| TI-V100 | Enable/disable | before start, `STARTING`, `ACTIVE`, after `RECORDING`, `STOPPING` | `IN_REVIEW` | zero-source start and last-source reconfigure fail closed; full transition matrix and provider-removal retry remain pending |
| TI-V110 | Permission/capability | grant/revoke at every state; OS/provider changes | `IN_REVIEW` | Wi-Fi scan prerequisites and permission surfaces now match the used APIs in unit/compile proof; dynamic revoke and API/OEM device execution remain |
| TI-V120 | Consent/privacy | epoch change, deletion, radio key rotation, export absence | `BLOCKED` | new radio event payloads withhold provider/subscription identity; full clear has an epoch fence, but legacy radio export, scoped reversal/dirty-day/export and purpose/epoch HMAC rotation are absent |
| TI-V130 | Transaction crash | manifest, desired action, acceptance, WAL admission, typed fact/membership, source cursor, history query, deletion fence | `IN_REVIEW` | WAL integrity/quarantine and the non-cancellation wrapper are proven locally; boundary-specific cancellation injection, single-writer mutation, typed destination identity, source cursor atomicity, process kill and post-accept recovery remain missing |
| TI-V140 | Process/clock | kill, reboot, wall jump, zone change, stale motion | `BLOCKED` | boot/elapsed lease generation is tested, but force-stop leaves session demands active, automation outbox lacks freshness generations, and wall-time uncertainty can fabricate an exact day |
| TI-V150 | Delivery faults | duplicate, reordered, delayed, poison, partition blockage/quarantine | `IN_REVIEW` | persisted WAL bytes are rehashed before decode; a corrupt raw ordinal is terminally quarantined and a healthy later ordinal advances. Parameterized cross-source and device delivery coverage remains |
| TI-V160 | Writer lifecycle | shadow, destination ownership, cutover, rollback, retirement, retained-floor replay | `IN_REVIEW` | At `9b8ab4b43`, Steps pending commands and the dormant typed lane carry stable source-local identity and exact owner generations; legacy admission is transactionally fenced; immutable service-run reads accept coherent caught-up retired lanes and fail closed on cutoff/retirement gaps, malformed lifecycle, stale epochs and retention loss. Contained host tests prove atomic candidate activation, contain/drain/retire rollback, and ABA-safe empty-generation re-arm. TI-B160 proves the same-process production deletion-service/barrier/re-arm seam and retained-state rerun. Cold process-death/device interleaves, continuous query/export/delete visibility, explicit release action, and every non-Steps transition remain unproven; Location remains protected. |
| TI-V170 | Android power/device | sleep, doze, restriction, saver, low storage, sensor absence | `NOT_STARTED` | device matrix required |
| TI-V180 | Platform variants | Android versions, OEMs, multi-SIM | `NOT_STARTED` | device matrix required |
| TI-V190 | Soak | repeated starts/stops, policy transitions, delayed providers | `NOT_STARTED` | soak harness required |
| TI-V200 | Policy rollback privacy | revoke source/purpose, disable v2 execution, reboot | `IN_REVIEW` | Room policy/consent epochs survive deletion; broker demand/registration rows are deleted; exact vector/current-policy/boot-effective-time callback fences and Activity purpose projection pass host tests; schema-capable rollback, reboot, demand rebuild, and global reconciliation remain pending |
| TI-V210 | Android trigger legality | crash after durable intent for every origin; expiry/new legal trigger | `BLOCKED` | trigger envelope schema/validator exists, but production propagation, current-epoch comparison, consumption, and pre-service durable gateway are missing |
| TI-V220 | Eligibility boundary | add/remove capture while shared control registration is unchanged; reorder callbacks | `IN_REVIEW` | demand add/remove rotates exact immutable generation/vector; generic and Activity delayed/pre-effective callbacks are rejected; replacement failure, process recovery, all-source global reconcile, and device interleave remain pending |
| TI-V230 | Accumulator boundary | kill/revoke/disable/re-enable Steps and Pressure mid-window | `IN_REVIEW` | Steps baselines and Pressure windows reset at generation/eligibility change and delayed callbacks are rejected; process-kill/device execution and completeness reporting remain pending |
| TI-V240 | Writer interleave | pause legacy writer, activate typed owner, race both commits | `IN_REVIEW` | At `9b8ab4b43`, the legacy Steps write and exact owner read share one transaction, queued ABA generations are immutable, missing authority cannot consume the command, and old/new service runs retain their immutable writer. Host tests cover closed callbacks/commands, legacy drain, atomic candidate activation, candidate receipt/cursor commit, contain/drain rollback, and deletion re-arm. TI-B160 adds the same-process production deletion-service/barrier/re-arm interleave. Cold process-death/device races, continuous production query/export/delete reads, and an explicit release action remain required. |
| TI-V250 | Correction chain | v1, duplicate, v2, delayed v1, delete, duplicate delete, retained-floor replay | `BLOCKED` | removed synthetic generic contribution tests do not count as shipping evidence. The first typed Steps fact/recompute path must prove stable identity, deterministic supersession, aggregate recompute and no resurrection before reuse by another source |
| TI-V260 | Retention resurrection | crash after destination deletion before WAL prune; activate backfill; rotate radio key | `BLOCKED` | policy/consent epoch fences and raw pruning foundations exist; atomic source/purpose deletion, typed-fact removal, dirty-day repair, applicable key rotation and delayed callback/import no-resurrection remain missing |
| TI-V270 | Policy-store bootstrap | crash before/after legacy read, target write, authority activation and rollback | `IN_REVIEW` | corrupt/unknown legacy state cannot become defaults or consent; Room publishes before mirror repair; observation failures replace stale replay with fail-closed state and retry; explicit kill-point/device cases remain |
| TI-V280 | History recompute lost wakeup | enqueue a newer correction during source/day recompute and crash around publish/ack | `BLOCKED` | a persisted universal dirty-day queue is not assumed; the chosen typed recompute/cache path must prove monotonic invalidation/ack if it introduces cached derived state |
| TI-V290 | Poison partition | poison Pressure N followed by valid Steps N+1 | `IN_REVIEW` | the raw coordinator path now quarantines corrupt ordinal N before projector code and advances to valid N+1; the focused proof uses Activity rows, so the exact cross-source Pressure→Steps scenario remains to be parameterized |
| TI-V300 | Production graph | provider callback through `TrackingHistoryRepository` and real Today/Timeline/Calendar/detail/map/export/deletion consumers | `BLOCKED` | `f9b1c3c45` proves a known-ID read-only Trip Detail subset and TI-B177–TI-B178 prove the bounded production Dashboard composition from durable host fixtures. `1bb9749af` compiles the connected provider-to-repository harness, but no device execution reaches that query. Today/Timeline/Calendar/map/export/deletion and broader numeric consumers remain unproven. |
| TI-V310 | Live Today rollover | long-lived collector crosses midnight, DST and zone policy change | `BLOCKED` | attribution ignores wall-time uncertainty; legacy `observeToday()` captures the date once and Dashboard uses local-midnight milliseconds divided by a fixed day length |

## Required UI/product states

| ID | State | Production query expectation | UI/accessibility gate | Status |
| --- | --- | --- | --- | --- |
| TI-VU01 | rich multi-source day | typed products and completeness for all present sources | hero/ribbon/sessions/between-session/explanations | `NOT_STARTED` |
| TI-VU02 | each six only-source days | source remains discoverable without Location/trip metrics | six hero goldens, navigation, non-color labels | `BLOCKED` |
| TI-VU03 | sessions, no ambient | explicit no-ambient policy/result, not missing/error | no fabricated ambient section | `NOT_STARTED` |
| TI-VU04 | ambient facts, no session | day exists with outside-session facts | no fabricated trip; explanation/export/delete reachable | `BLOCKED` |
| TI-VU05 | nothing enabled | sealed policy state | actionable enable path | `BLOCKED` |
| TI-VU06 | enabled, no observation | explicit evidence state, no zero metric | provider/capability explanation | `BLOCKED` |
| TI-VU07 | permission/capability failure | unavailable/OS-limited with named cause | repair action and TalkBack label | `BLOCKED` |
| TI-VU08 | materializing/backfilling | recorded evidence distinct from metric readiness | progress/reduced-motion state | `IN_REVIEW`; immediate selected-detail `Materializing` is host-tested, but cross-surface, device, visual, reduced-motion, and accessibility proof is absent |
| TI-VU09 | partial historical day | ambiguous facts absent, completeness `PARTIAL` | no invented zeros; explanation | `BLOCKED` |
| TI-VU10 | degraded current session | accepted sources plus named rejected causes | non-color status and repair action | `BLOCKED` |
| TI-VU11 | cross-midnight continuation | stable structural day keys and exact slices | continuation semantics, navigation both days | `BLOCKED` |
| TI-VU12 | accessibility/layout | same production state at 200% text/adaptive sizes | TalkBack order, 48dp targets, reduced motion, screenshots/performance | `NOT_STARTED` |

## Baseline evidence

| ID | Command | Result | Classification |
| --- | --- | --- | --- |
| TI-B001 | `.\gradlew.bat testDebugUnitTest --no-daemon --stacktrace --console=plain` | `BUILD SUCCESSFUL in 4m 55s`; 957 tasks, 8 executed/949 up-to-date. Earlier concurrent missing-results run is discarded. | passing baseline |
| TI-B002 | `.\gradlew.bat :app:assembleDebug lintDebug --no-daemon --stacktrace --console=plain` | `:app:assembleDebug` passed; `:app:lintDebug` failed with 43 errors/48 warnings on the untouched checkout. First failure: `values-ga-rIE/strings.xml:67` lacks required Irish plural quantities. | pre-existing lint failure; assembly baseline passing |
| TI-B003 | `.\gradlew.bat :app:assembleRelease :app:assembleRelease_nominify --no-daemon --stacktrace --console=plain` | `BUILD SUCCESSFUL in 11m 51s`; 1178 tasks | passing release assembly baseline |
| TI-B003A | `.\gradlew.bat :app:lintRelease --no-daemon --stacktrace --console=plain` | failed with the same 43 pre-existing app errors and 45 warnings; first failure is the Irish plural resource at `values-ga-rIE/strings.xml:67` | pre-existing release lint failure |
| TI-B004 | `.\gradlew.bat checkRoomSchemaDrift --no-daemon --stacktrace --console=plain` | `BUILD SUCCESSFUL in 24s`; 46 tasks | passing baseline |
| TI-B005 | `.\gradlew.bat :app:connectedDebugAndroidTest` | not run; device not established | device blocker |
| TI-B006 | `.\gradlew.bat :tracker:engine:testDebugUnitTest --tests "*BackgroundTrackingApiLogicTest" ...` | `BUILD SUCCESSFUL in 2m 47s` after inactive-`STILL` regression added | passing containment verification |
| TI-B007 | `.\gradlew.bat :tracker:engine:testDebugUnitTest :tracker:engine:lintDebug ...` | `BUILD SUCCESSFUL in 6m 34s`; full module tests pass, lint has two existing warnings | passing scoped integration gate |
| TI-B008 | `.\gradlew.bat :core:base:testDebugUnitTest :tracker:engine:testDebugUnitTest :core:base:compileDebugAndroidTestKotlin --no-daemon` | `BUILD SUCCESSFUL in 6m 17s` after correcting the v27→v28 column placement; 255 tasks | passing policy/migration compatibility gate |
| TI-B009 | `.\gradlew.bat :data:preferences:testDebugUnitTest :data:preferences:lintDebug --no-daemon` | `BUILD SUCCESSFUL in 1m 25s`; policy/bootstrap tests pass; lint has only seven pre-existing missing-translation errors | passing scoped policy gate with recorded baseline findings |
| TI-B010 | `.\gradlew.bat checkRoomSchemaDrift --no-daemon` | fails only because the newly generated required `28.json` is untracked; no commit/staging was authorized | expected integration handoff, not a schema-content regression |
| TI-B011 | `.\gradlew.bat :app:connectedDebugAndroidTest` | not run; no connected device established, so `AppDatabaseMigration27To28Test` is compile-only | device verification pending |
| TI-B012 | `.\gradlew.bat :data:preferences:compileDebugKotlin :tracker:engine:compileDebugKotlin :tracker:engine:compileDebugUnitTestKotlin :data:preferences:compileDebugUnitTestKotlin --no-daemon` | `BUILD SUCCESSFUL in 1m 23s` after atomic policy/WAL and consent-ledger validation changes | passing compile gate |
| TI-B013 | focused policy/ingress/coordinator tests across `:data:preferences` and `:tracker:engine` | `BUILD SUCCESSFUL in 38s`; corrupt/unknown legacy, mirror blocking, replay failure, ledger corruption, stale WAL authorization, and coordinator policy tests pass | passing adversarial regression gate |
| TI-B014 | `.\gradlew.bat :core:base:testDebugUnitTest :data:preferences:testDebugUnitTest :tracker:engine:testDebugUnitTest :core:base:compileDebugAndroidTestKotlin :app:testDebugUnitTest :app:assembleDebug --no-daemon` | `BUILD SUCCESSFUL in 5m 52s`; 737 tasks, exact migration test compiles and debug APK assembles | passing local integration gate; migration remains device-unrun |
| TI-B015 | `.\gradlew.bat :core:base:lintDebug :data:preferences:lintDebug :tracker:engine:lintDebug :app:lintDebug --no-daemon`, then `.\gradlew.bat :app:lintDebug --no-daemon` | core/data/engine scoped lint tasks pass with recorded baselines. After correcting three introduced findings, the app-only rerun reports exactly the untouched baseline: 43 errors/48 warnings, first at the pre-existing Irish plural resource; no changed policy/UI file appears in the report. | scoped changes clean; repository app-lint baseline remains failing |
| TI-B016 | `:tracker:engine:compileDebugKotlin :tracker:engine:compileDebugUnitTestKotlin` | `BUILD SUCCESSFUL in 2m 6s`; expanded lifecycle production and unit sources compile | passing lifecycle compile gate |
| TI-B017 | focused `AuthoritativeSessionCoordinatorTest`, `RoomDurableSourceIngressTest`, `TrackerServiceSourceSessionTest`, and `ForceStopSourceSessionFinalizerTest` | `BUILD SUCCESSFUL in 46s`; 29 tests pass after adding manifest/lease, cutoff, terminal-intent, time-order, and lease-ABA regressions | passing focused lifecycle regression gate; not Phase 1 completion |
| TI-B018 | `:core:base:testDebugUnitTest :data:preferences:testDebugUnitTest :tracker:engine:testDebugUnitTest` serialized broad suite | core and preferences completed and engine tests ran, but Gradle failed while reading `in-progress-results-generic.bin` with `NoSuchFileException`; no assertion failure was reported | infrastructure failure; not counted as passing or regression evidence |
| TI-B019 | `:core:base:compileDebugAndroidTestKotlin :tracker:engine:lintDebug` | `BUILD SUCCESSFUL in 4m 21s`; expanded migration test compiles; engine lint reports only its two recorded warnings | passing scoped compile/lint gate |
| TI-B020 | Android SDK `adb devices` | no devices attached | exact v27→v28 instrumentation execution remains blocked |
| TI-B021 | `checkRoomSchemaDrift` after lifecycle schema expansion | fails only on `?? core/base/schemas/.../28.json`; the generated schema is intentionally untracked because commit/staging was not authorized | expected handoff state, not a schema-content mismatch |
| TI-B022 | final focused coordinator and durable-ingress rerun after STOPPING lease-generation fencing | `BUILD SUCCESSFUL in 1m 29s` | passing final code-path regression rerun |
| TI-B023 | `:tracker:engine:testDebugUnitTest :tracker:engine:lintDebug` isolated final module pass | `BUILD SUCCESSFUL in 7m 27s`; complete tracker-engine unit suite passes and lint reports only its recorded two warnings | passing final tracker-engine regression gate |
| TI-B024 | focused broker, registration repository, durable ingress, coordinator, Activity arbiter/receiver, Steps, and Pressure tests | `BUILD SUCCESSFUL in 3m 15s`; 281 tasks | passing initial Phase 2 component gate; not scenario/product proof |
| TI-B025 | stop-drain and automatic-purpose focused rerun | `BUILD SUCCESSFUL in 3m 48s`; 281 tasks | passing adversarial correction gate |
| TI-B026 | effective boot/time eligibility, deletion survival, migration-test compilation, Activity admission, and source callback fencing | `BUILD SUCCESSFUL in 3m 32s`; 290 tasks | passing v28 broker boundary gate; migration remains device-unrun |
| TI-B027 | fresh Android adversary: complete `:tracker:engine:testDebugUnitTest` | `BUILD SUCCESSFUL in 2m 44s`; 232 tasks | independent host regression pass |
| TI-B028 | broad `:core:base`/`:tracker:engine`/`:sensor:activity` unit+lint run | tests/lint ran, then Gradle lost `in-progress-results-generic.bin`; no assertion failure. Activity lint clean; engine retained 2 warnings; core retained 13 existing MissingTranslation errors | infrastructure failure plus recorded lint baseline; not a clean broad pass |
| TI-B029 | `:app:assembleDebug` | `BUILD SUCCESSFUL in 2m 9s`; 654 tasks | passing full debug assembly integration gate |
| TI-B030 | `checkRoomSchemaDrift` after broker schema expansion | fails only on `?? core/base/schemas/com.adsamcik.tracker.shared.base.database.AppDatabase/28.json` | expected required untracked schema handoff |
| TI-B031 | Android SDK `adb devices` | no devices attached | migration/provider/reboot/Doze/OEM tests remain blocked |
| TI-B032 | historical focused generic Phase 3 seam tests | `BUILD SUCCESSFUL in 36s` before the R2 rescope | superseded by TI-D051; the synthetic-only repositories/tests and 12 generic tables were removed from unreleased v28 |
| TI-B033 | `:tracker:engine:testDebugUnitTest --tests "*RoomDurableSourceIngressTest" :core:base:compileDebugAndroidTestKotlin` | first run found provider-dedup retry collision; corrected rerun `BUILD SUCCESSFUL in 2m 28s`, 14 tests; migration test compiles | passing ingress/migration-compile regression gate; device migration pending |
| TI-B034 | `:core:base:testDebugUnitTest` | `BUILD SUCCESSFUL in 1m 3s`; full module unit suite passes after Phase 3 integration | passing core-base host regression; not device/product proof |
| TI-B035 | fresh pre-wiring R1b: three independent read-only `gpt-5.6-sol` high-reasoning reviews plus lead repository-reference verification | verdict `BLOCKED`; new payload-integrity, counting-day move, force-stop-demand, attribution-drift, cancellation, cross-source floor, wall-time uncertainty, and Today rollover findings recorded | adversarial gate failed; no materializer/day UI wiring authorized |
| TI-B036 | `:core:base:testDebugUnitTest` after removing the generic Phase 3 platform | `BUILD SUCCESSFUL in 1m 15s`; 75 tasks | passing reshaped-v28 core regression; not device/product proof |
| TI-B037 | focused `DatabaseImportTest` and `DatabaseImportCollisionTest` | corrected rerun `BUILD SUCCESSFUL in 10s`; 13 tests | hostile backup imports allowed user facts and excludes foreign policy authority/ACTIVE demand |
| TI-B038 | focused WAL ingress and projection tests | `BUILD SUCCESSFUL in 2m 07s` | valid-but-mutated payload rejected before decode; raw quarantine bypasses projector and advances contiguous progress |
| TI-B039 | focused `TrackingCoordinatorTest` | `BUILD SUCCESSFUL in 1m 20s` | corrupt ordinal 1 quarantined and healthy ordinal 2 applied; terminal failure/checkpoint evidence persisted |
| TI-B040 | focused `RoomSourcePolicyRepositoryTest` plus authoritative settings tests | `BUILD SUCCESSFUL in 17s`; 108 tasks | automatic legacy Activity-control bootstrap and mode grant/revoke epochs pass without capture grant |
| TI-B041 | `:core:base:compileDebugAndroidTestKotlin` | `BUILD SUCCESSFUL in 2s`; 48 tasks | reshaped v27→v28 instrumentation sources compile; no device attached for execution |
| TI-B042 | `:app:assembleDebug` | `BUILD SUCCESSFUL in 19s`; 650 tasks | passing full debug assembly after v28 rescope |
| TI-B043 | `checkRoomSchemaDrift` | fails only on `?? core/base/schemas/com.adsamcik.tracker.shared.base.database.AppDatabase/28.json` | expected untracked generated schema handoff; no staging/commit authorized |
| TI-B044 | repository-wide `testDebugUnitTest` | `BUILD FAILED in 6m 5s`; `:app:testDebugUnitTest` has 205/579 failures rooted in `UnsatisfiedLinkError: no sqliteX in java.library.path` with cascading coroutine pre-test failures | host-native test-harness baseline; focused changed-area suites pass, but broad gate is not claimed green |
| TI-B045 | Android SDK `adb devices` | no devices attached | exact populated migration/open/reopen and provider/reboot execution remain blocked |
| TI-B046 | `:core:base:lintDebug :data:preferences:lintDebug :tracker:engine:lintDebug :feature:import-export:lintDebug` | `BUILD SUCCESSFUL in 1m 30s`; 486 tasks | changed-module lint gate passes; reports recorded baseline findings (core 13 errors, preferences 7 errors/2 warnings, import-export 4 errors/7 warnings) and tracker 2 warnings |
| TI-B047 | focused lean-model, authoritative coordinator and raw coordinator tests plus migration-test compilation | `BUILD SUCCESSFUL in 2m 14s`; 241 tasks; 21/21 tests pass | executable TI-D052 comparison complete; `lifecycle_reconcile_lease` removed and shared `source_coordinator_lease` upgraded to boot/process-aware monotonic generations |
| TI-B048 | `:tracker:engine:testDebugUnitTest --tests "*ConnectivityObservationAdmissionTest" --tests "*ConnectivityPrerequisiteEvaluatorTest" --tests "*ConnectivityWakeupPolicyTest"` | `BUILD SUCCESSFUL in 2m 3s`; 228 tasks | passing first TI-310 containment gate: mixed-age/unknown/future radio children fail closed, bounded interleaved live replay is rejected only after durable handoff, new provider time remains admissible, failed Wi-Fi updates skip cache reads, and unsupported Cell refresh creates no polling loop; durable cross-process identity and device proof remain blocked |
| TI-B049 | `:tracker:engine:testDebugUnitTest --tests "*ConnectivityObservationAdmissionTest" --tests "*ConnectivityPrerequisiteEvaluatorTest" --tests "*SourcePayloadCodecTest" --tests "*NonCancellationResultTest"` | `BUILD SUCCESSFUL in 2m 3s`; 228 tasks | expanded TI-310/TI-312 gate: confirmed-empty coverage, identity minimization, Wi-Fi payload-v2 item time/v1 compatibility, actual scan permission contract, and cancellation propagation |
| TI-B050 | `:app:compileDebugKotlin` then `:app:processDebugMainManifest` | compile `BUILD SUCCESSFUL in 59s` (350 tasks); manifest `BUILD SUCCESSFUL in 18s` (63 tasks) | onboarding/settings Wi-Fi permission correction compiles and the merged debug manifest accepts removal of the unused Nearby Wi-Fi permission |
| TI-B051 | `:tracker:engine:testDebugUnitTest --tests "*ConnectivityObservationAdmissionTest" --tests "*ConnectivityPrerequisiteEvaluatorTest" --tests "*SourcePayloadCodecTest" --tests "*NonCancellationResultTest"` | `BUILD SUCCESSFUL in 1m 52s`; 228 tasks (15 executed, 213 up-to-date) | TI-D083 containment proof: paid acquisition budgets are finite, direct-session-capture-only and close on qualified evidence; prior freshness/zero/minimization/codec/cancellation assertions remain green. Durable restart identity, runtime fakes, calibrated policy values and device energy remain blocked. |
| TI-B052 | historical local `:feature:dashboard:testDebugUnitTest --tests "*DashboardManualStartDecisionTest"` | `BUILD SUCCESSFUL in 8s`; 262 tasks (2 executed, 260 up-to-date); 17 assertions, but the test file is protected user-owned untracked work | Non-gate historical evidence only. The result cannot be reproduced from committed HEAD and must not close TI-213 unless equivalent repository-owned assertions are implemented and rerun; the protected file is never staged by this program. |
| TI-B053 | `:tracker:engine:testDebugUnitTest --tests "*ConnectivityObservationAdmissionTest" --tests "*ConnectivityPrerequisiteEvaluatorTest" --tests "*SourcePayloadCodecTest" --tests "*NonCancellationResultTest" --tests "*AuthoritativeSessionCoordinatorTest"` | `BUILD SUCCESSFUL in 2m 29s`; 232 tasks (15 executed, 217 up-to-date) | conservative one-request radio containment remains green; cancelled provider start/reconfigure now propagates without provider close or terminal failure conversion. Atomic sequence admission and remaining cancellation injection boundaries remain TI-312 blockers. |
| TI-B054 | `:app:compileDebugKotlin` | `BUILD SUCCESSFUL in 16s`; 346 tasks (6 executed, 340 up-to-date) | integrated app graph compiles after the source-aware Dashboard start decision, radio containment and coordinator cancellation changes |
| TI-B055 | `:core:base:testDebugUnitTest --tests "*SourceBrokerDaoTest" --tests "*SourcePolicyDeletionSurvivalTest"` | `BUILD SUCCESSFUL in 56s`; 79 tasks | exact/post deny-all selection, pre-boundary authorization, half-open physical retirement, and full-deletion clearing of broker authority pass |
| TI-B056 | `:tracker:engine:testDebugUnitTest --tests "*SourceBrokerTest" --tests "*SourceRegistrationRepositoryTest"` | `BUILD SUCCESSFUL in 2m 17s`; 232 tasks | automatic-control revoke appends an exact deny boundary; authority-only/global-plan changes rotate authorization without physical generation churn; configuration change rotates physical generation |
| TI-B057 | `:sensor:activity:testDebugUnitTest --tests "*DefaultActivityRegistrationArbiterTest" --tests "*ActivityReceiverTest"` | `BUILD SUCCESSFUL in 40s`; 207 tasks | unchanged GMS configuration receives one provider apply across purpose/manifest/policy changes; PendingIntent/receiver carry physical identity and defer observed-time purpose resolution |
| TI-B058 | `:core:base:testDebugUnitTest --tests "*SourceEventWalDaoTest"` | `BUILD SUCCESSFUL in 27s`; 79 tasks | WAL checksum/integrity and pending-legacy classification host assertions pass; released legacy projection drain is not proven |
| TI-B059 | `:core:base:compileDebugAndroidTestKotlin` | `BUILD SUCCESSFUL in 13s`; 48 tasks | migration instrumentation now expects `source_authorization`, asserts the retired eligibility table is absent, and compiles; no device execution |
| TI-B060 | focused `RoomDurableSourceIngressTest` and `RoomActivityRecognitionEventIngressTest` | initial run exposed five superseded receipt-time/session-sink assertions; after preserving observation-first semantics and deriving attribution only from observed authorization, rerun `BUILD SUCCESSFUL in 2m 2s`; 228 tasks | pre-boundary evidence remains durable; exact deny rejects; control-only observations persist without session attribution; forged caller policy/session stamps cannot relabel WAL; cross-boundary windows fail closed |
| TI-B061 | isolated `AuthoritativeSessionCoordinatorTest.revocation committed during provider start rolls registration back` | initial stale mock expected receipt-time rejection; corrected observed-time scenario rerun `BUILD SUCCESSFUL in 1m 43s`; 228 tasks | a valid pre-revocation observation remains durable while the coordinator still rolls back a just-started provider after current-policy recheck |
| TI-B062 | checked-in Room schema 27/28 name-set audit | v27 `51`, v28 `61`; all 51 v27 tables preserved; new tables are the ten documented authority/lifecycle/broker records; `provider_registration_eligibility` absent | unreleased-v28 structural slice matches the recorded table boundary; semantic migration remains unexecuted |
| TI-B063 | `:app:compileDebugKotlin` | `BUILD SUCCESSFUL in 49s`; 346 tasks | integrated app graph compiles after observed-time authorization, Activity identity, admission, migration-test and sole-source changes |
| TI-B064 | `git diff --check` after code, test and execution-artifact integration | pass; no output | edited patch is whitespace-clean; the intentionally dirty pre-existing/user worktree remains preserved and uncommitted |
| TI-B065 | `:core:base:testDebugUnitTest :data:preferences:testDebugUnitTest :tracker:engine:testDebugUnitTest :sensor:activity:testDebugUnitTest :feature:dashboard:testDebugUnitTest :feature:import-export:testDebugUnitTest :core:base:compileDebugAndroidTestKotlin :app:compileDebugKotlin` | `BUILD SUCCESSFUL in 5m 5s`; 597 tasks (17 executed, 580 up-to-date) | passing combined changed-module JVM/compile baseline before the first code commit |
| TI-B066 | `:core:base:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test` on `Medium_Phone` | `BUILD SUCCESSFUL in 1m 58s`; one test; 108 tasks | proves the local instrumentation/migration harness executes; current shallow fixture preserves unsafe legacy-active state and therefore does not satisfy the populated migration/recovery gate |
| TI-B067 | focused `SourceBrokerDaoTest`, `SourceRegistrationRepositoryTest`, `SourceBrokerTest`, `RoomActivityRecognitionEventIngressTest`, and `DefaultActivityRegistrationArbiterTest` | implementation run `BUILD SUCCESSFUL in 1m 42s` (277 tasks); integration-owner rerun `BUILD SUCCESSFUL in 9s` (277 tasks) | shared accepted-before-retire pointer swap and Activity make-before-break pass failed-replacement, stale-cleanup, repeated-clear, same-instance handoff and mismatched-instance rejection assertions; other provider adapters remain gated |
| TI-B068 | `:sensor:activity:testDebugUnitTest --tests "*DefaultActivityRegistrationArbiterTest"` after first-acceptance cancellation injection | `BUILD SUCCESSFUL in 37s`; 11 tests, zero failures/errors/skips | cancellation during a non-cancellable accepted swap propagates only after in-memory snapshot, durable pointer and ACTIVE generation converge |
| TI-B069 | `:app:assembleDebug` | `BUILD SUCCESSFUL in 59s`; 650 tasks (36 executed, 614 up-to-date) | debug APK assembles after final Activity handoff corrections |
| TI-B070 | pre-staging `checkRoomSchemaDrift` | expected failure only on `?? core/base/schemas/com.adsamcik.tracker.shared.base.database.AppDatabase/28.json`; assembly run separately and passes as TI-B069 | generated unreleased-v28 schema must be included in the foundation commit; this is not a schema-content mismatch |
| TI-B071 | post-commit `checkRoomSchemaDrift` | `BUILD SUCCESSFUL in 7s`; 42 tasks (3 executed, 39 up-to-date) | committed unreleased-v28 Room schema is drift-free at the foundation boundary |
| TI-B072 | `:core:base:compileDebugAndroidTestKotlin` for populated migration fixture | `BUILD SUCCESSFUL in 11s`; 48 tasks | exact-v27 six-source/runtime/WAL fixture and production reopen/deletion assertions compile |
| TI-B073 | populated `AppDatabaseMigration27To28Test` on `Medium_Phone` | implementation final `BUILD SUCCESSFUL in 23s`; integration-owner rerun `BUILD SUCCESSFUL in 17s`; 1/1 test; 104 tasks | migrate/close/production-Room reopen/query/delete/reopen preserves released facts, terminalizes unprovable runtime, leaves v28 authority fail-closed, proves FK Location-child cascade, and shows no deletion resurrection |
| TI-B074 | `:core:base:testDebugUnitTest checkRoomSchemaDrift` | implementation run `BUILD SUCCESSFUL in 53s` (89 tasks); integration-owner rerun `BUILD SUCCESSFUL in 9s` (85 tasks) | core regression and committed schema guard remain green after migration terminalization |
| TI-B075 | `:core:base:lintDebug` | `BUILD SUCCESSFUL in 1m 17s`; 123 tasks | populated migration slice adds no unfiltered lint regression |
| TI-B076 | focused Tracker-database collision, ZIP-classification and import-job retry tests, followed by `:feature:import-export:testDebugUnitTest :feature:import-export:lintDebug` | implementation commands `BUILD SUCCESSFUL`; complete module suite 317 tests, 0 failures/errors/skips; integration-owner focused reruns `BUILD SUCCESSFUL in 21s` and `33s` after the explicit test-hook cleanup (175 tasks); `git diff --check` clean | full Tracker databases and backup ZIPs fail before merge mutation, never become zero-row `COMPLETE`, remain reselectable, and ordinary database/archive imports retain their prior path; actual cold-start restore and provenance-fenced partial merge remain blocked |
| TI-B077 | `:tracker:engine:testDebugUnitTest --tests "*RoomDurableSourceIngressTest" :core:base:testDebugUnitTest --tests "*SourceEventWalDaoTest"` | implementation run `BUILD SUCCESSFUL in 2m 45s`; first integration-owner rerun `BUILD SUCCESSFUL in 10s`; physical-retirement rerun `BUILD SUCCESSFUL in 1m 54s`; final cancellation-injection rerun `BUILD SUCCESSFUL in 1m 57s`; 242 tasks | process-stable delivery replay/collision, sparse authorization filtering, mixed authorization revisions, authorization/physical-boundary rejection, delayed retired-generation evidence, retention-floor filtering, encode-cancellation propagation, and post-insert transactional WAL/sequence rollback pass; source adapters remain |
| TI-B078 | populated `AppDatabaseMigration27To28Test` on `Medium_Phone` after delivery-envelope expansion | implementation run `BUILD SUCCESSFUL in 28s`; integration-owner rerun `BUILD SUCCESSFUL in 20s`; 1/1 test; 104 tasks on the final rerun | additive delivery metadata validates on the direct unreleased-v28 migration while all released-v27 facts and terminalization/deletion assertions remain green |
| TI-B079 | `:app:assembleDebug` | `BUILD SUCCESSFUL in 1m 5s`; 650 tasks (50 executed, 600 up-to-date) | full debug APK integrates the atomic delivery-admission substrate |
| TI-B080 | `:core:base:lintDebug :tracker:engine:lintDebug` | `BUILD SUCCESSFUL in 1m 26s`; 378 tasks | changed-module lint gate passes; reports retain the recorded core localization baseline and two existing tracker-engine warnings, with no new delivery-file finding |
| TI-B081 | `:app:compileDebugKotlin :tracker:engine:lintDebug` after binding `SourcePayloadCodec` at the interface boundary | `BUILD SUCCESSFUL in 1m 27s`; 588 tasks | final app dependency graph compiles; tracker-engine lint retains only its two recorded warnings |
| TI-B082 | post-commit `checkRoomSchemaDrift` | `BUILD SUCCESSFUL in 7s`; 42 tasks | committed unreleased-v28 delivery-envelope schema is drift-free |
| TI-B083 | fresh read-only Activity atomic-adapter adversarial review | two commit-local HIGH findings accepted and mitigated; no other BLOCKER/HIGH/MEDIUM survived | non-throwing recovery failure/incomplete ordinal now withholds selection, motion and backend publication; exact duplicate replay reloads/drains but has zero transient effect |
| TI-B084 | `:tracker:engine:testDebugUnitTest --tests "*ActivitySourceDeliveryFactoryTest" --tests "*RoomActivityRecognitionEventIngressTest" --tests "*RoomDurableSourceIngressTest" :sensor:activity:testDebugUnitTest --tests "*ActivityReceiverTest" --tests "*ActivityReceiverThreadSafetyTest" --no-daemon` | `BUILD SUCCESSFUL in 3m 48s`; 267 tasks; 47 tests, zero failures/errors/skips | canonical order-independent delivery identity, sparse original-index selection, exact writer-row reload, real Room pre-registration filtering/stamping, duplicate zero-effect, four incomplete-recovery outcomes and receiver cache/backend containment pass |
| TI-B085 | `:tracker:engine:testDebugUnitTest :sensor:activity:testDebugUnitTest --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 7m 18s`; 267 tasks | complete changed-module unit regression passes after adversarial corrections and real Room adapter integration |
| TI-B086 | `:sensor:activity-api:lintDebug :sensor:activity:lintDebug :tracker:engine:lintDebug :app:assembleDebug --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 3m 17s`; 881 tasks | Activity API/module lint and tracker-engine lint complete, debug APK assembles; Activity has no findings and tracker-engine retains its two recorded warnings |
| TI-B087 | `:tracker:engine:testDebugUnitTest --tests "*RoomActivityRecognitionEventIngressTest" --no-daemon` after reversing the mocked admission-unit return order | `BUILD SUCCESSFUL in 2m 27s`; 228 tasks | adapter canonicalizes returned unit order before exact row reload, motion application and original-index selection |
| TI-B088 | three independent read-only released-v27 adversaries: projection/migration correctness, Android startup/drain ordering, and restore/import scope | aggregate `BLOCKED / RESCOPE NARROWLY`; exact containment boundary accepted; process-wide startup fence, frozen drain, production backup-wrapper proof and partial-database merge containment remain blocking | prevents ordinary v2 replay, ghost automation, hot restore, or a generic compatibility framework from being mistaken for the next safe slice |
| TI-B089 | `:core:base:testDebugUnitTest --tests '*SourceEventWalDaoTest' --no-daemon --no-parallel` after adversarial retention correction | `BUILD SUCCESSFUL in 1m 12s`; 75 tasks | pending v1 effect pins its originating WAL after the projector checkpoint; terminal disposition releases WAL and boundedly removes the effect payload |
| TI-B090 | `:core:base:testDebugUnitTest :tracker:engine:testDebugUnitTest --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 8m 17s`; 246 tasks | complete affected-module unit regression passes for recovery records, high-watermark activation, frozen decoder, v1/v2 isolation, outbox filtering and retention cleanup |
| TI-B091 | `:core:base:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test" --no-daemon --no-parallel` on `Medium_Phone` | `BUILD SUCCESSFUL in 33s`; four tests; 104 tasks | production reopen/query/delete/reopen plus unknown projection, pruned-WAL sequence high-watermark and outbox-only recovery-obligation cases validate the final v28 containment schema |
| TI-B092 | `:core:base:lintDebug :tracker:engine:lintDebug :app:assembleDebug --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 5m 5s`; 860 tasks | debug APK integrates; core retains its recorded 13 missing-translation findings and tracker-engine its two recorded warnings, with no changed-file lint finding |
| TI-B093 | fresh final-diff `gpt-5.6-sol` containment adversary | no commit-local `BLOCKER` or `HIGH` finding; read-only | independently confirms SQL/entity/schema parity, exact v1 targets, high-watermark/floor behavior, v1/v2 and outbox isolation, retention/cleanup, deletion and golden decoder evidence; startup/drain runtime remains explicitly outside this commit |
| TI-B094 | `:tracker:engine:testDebugUnitTest --tests "*LegacyV27ProjectionRecoveryTest" --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 1m 50s`; 18 tests, zero failures/errors/skips; 228 tasks | destination-time retention, unregistered raw suppression, pre-activation/nonpositive outbox quarantine, WAL/outbox identity and frozen semantic equality, poison isolation, partial provenance, sparse replay, Location protection and exact retry pass |
| TI-B095 | `:core:base:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test" --no-daemon --no-parallel` on `Medium_Phone` | `BUILD SUCCESSFUL in 40s`; seven tests; 104 tasks | pure outbox ordinal 7 with no WAL row or sequence now defines the immutable cutoff; retained-floor and activation-floor destination recovery plus prior populated/query/delete boundaries pass |
| TI-B096 | `:core:base:testDebugUnitTest :tracker:engine:testDebugUnitTest --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 4m 55s`; 242 tasks | complete affected-module regression is green after destination-time retention, activation/semantic quarantine and cutoff corrections |
| TI-B097 | `:core:base:lintDebug :tracker:engine:lintDebug :app:assembleDebug checkRoomSchemaDrift --no-daemon --no-parallel` | `BUILD SUCCESSFUL in 3m 6s`; 861 tasks | Room schema is drift-free, debug APK assembles, core retains its recorded 13 localization findings and tracker-engine its two recorded warnings, with no changed-file lint finding |
| TI-B098 | fresh correction-only `gpt-5.6-sol` adversary | no new commit-local `BLOCKER` or `HIGH`; read-only | independently confirms outbox-inclusive cutoff, ACTIVE-only raw bridge, pre-activation/nonpositive quarantine, exact frozen semantic pairing, destination-time retention and continued Location isolation; process-wide startup caller remains explicitly blocking |
| TI-B099 | three fresh R1 read-only reviews: data/migration, Android/power/privacy, and product/scope | aggregate `BLOCK` on 2026-08-24 | R1-DM01/R1-DM02 accepted for immediate correction; source reachability, sole-source QoS floors, bounded crash-auditable provider handoff, retirement/re-arm, Cell replay, export minimization, immutable ambient day identity, and truthful `ACTIVE`/`RECORDING` remain blocking; R1-DM05 remains an explicit disagreement |
| TI-B100 | focused `:core:common`, `:data:preferences`, and `:tracker:engine` host shard for canonical boot clock and Activity epoch fencing | `BUILD SUCCESSFUL` on 2026-08-24 | canonical BOOT_COUNT/process fallback, policy effective time, registration revocation, Activity delivery/ingress/epoch/outbox/action/coordinator, and force/explicit/previous-exit finalizer assertions pass; proves local mitigation of R1-DM01/R1-DM02 only, not the R1 gate |
| TI-B101 | `:core:base:testDebugUnitTest` and `:core:base:compileDebugAndroidTestKotlin` | both `BUILD SUCCESSFUL` on 2026-08-24 | complete core database host suite passes and the v28 migration instrumentation source compiles; no `adb` executable/device is available in this environment, so connected migration execution remains unverified by this checkpoint |
| TI-B102 | isolated Git-index export of `f14a4a2b1` candidate, then `:app:testDebugUnitTest` startup/deletion/recovery shard + `:tracker:api:testDebugUnitTest` service launch/redelivery shard + `:tracker:engine:testDebugUnitTest` lifecycle/rollout/Activity-action/permission/shared-Steps/source-floor shard, `--no-daemon --no-parallel --console=plain` | `BUILD SUCCESSFUL in 5m 39s`; 613 actionable tasks (364 executed, 249 from cache); `git diff --cached --check` clean before commit; protected files absent from the 168-file index | proves the committed sources compile and the named host contracts pass without borrowing unstaged work. It does not prove full JVM regression, connected process/reboot/FGS behavior, a source materializer, production history query, device energy, or rollout readiness. |
| TI-B103 | `:core:base:testDebugUnitTest --tests '*TrackerRunDaoTest' --tests '*TrackerRunTest' :core:base:compileDebugAndroidTestKotlin` after the v27-boundary correction | `BUILD SUCCESSFUL in 28s`; 88 tasks | migrated unknown runtime completion/end remains unknown while terminal state plus `legacy_runtime_fenced` prevents active lookup, close, end, or resurrection. |
| TI-B104 | `:core:base:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test"` on `Medium_Phone(AVD) - 16` | `BUILD SUCCESSFUL in 37s`; seven tests | the corrected unreleased-v28 migration preserves all populated v27 facts, preserves null/known factual runtime boundaries, production-reopens, deletes, and does not resurrect data. |
| TI-B105 | post-commit `checkRoomSchemaDrift` after `7df895d24` | `BUILD SUCCESSFUL in 6s`; 42 tasks | the corrected v28 Room schema, entity, DAO, and migration boundary are checked in and drift-free. |
| TI-B106 | focused tracker-engine `TrackingRolloutStateTest`, `SourceBrokerTest`, `SourceRegistrationRepositoryTest`, `SharedStepSourceControllerTest`, `AutomaticStartTransitionMonitorTest`, and `SourcePipelineRecoveryTest` | `BUILD SUCCESSFUL in 3m 31s`; 232 tasks | contained rollout blocks physical reservation/acceptance and stale app control; Steps control cannot orphan a sensor; live drains participate in the deletion generation and withhold post-closure Activity effects. |
| TI-B107 | `:sensor:activity:testDebugUnitTest --tests '*DefaultActivityRegistrationArbiterTest' :sensor:activity:compileDebugKotlin` | `BUILD SUCCESSFUL in 1m`; 207 tasks | Activity cold re-arm and reserve→external apply→accept recheck rollout/policy; stale accepted authority is removed/fenced and unavailable automatic control is reported `BLOCKED`. |
| TI-B108 | `:app:testDebugUnitTest --tests '*DefaultTrackingStartupGateTest' --tests '*CollectedDataDeletionServiceTest'` | `BUILD SUCCESSFUL in 1m 26s`; 573 tasks | startup/deletion barrier ordering and generation quiescence remain green after enrolling live source drains. |
| TI-B109 | `:app:testDebugUnitTest --tests '*ArchitecturalFitnessTest'` | 29 tests across ten suites; zero failures, errors, or skips in the generated XML results | architecture boundaries, including the collected-data worker startup fence, remain green after the corrected R1 slices. |
| TI-B110 | focused `MultiSessionLifecycleTest`, `SessionCrashRecoveryTest`, and `EventTrackingFrameProjectionTest` | `BUILD SUCCESSFUL in 2m 13s`; 232 tasks | integration fixtures now declare explicit event-shadow acquisition and a hardware counter reset establishes a zero baseline instead of fabricating the new absolute count as steps. |
| TI-B111 | `:tracker:engine:testDebugUnitTest :sensor:activity:testDebugUnitTest --no-daemon --no-parallel --console=plain` | `BUILD SUCCESSFUL in 6m 52s`; 263 tasks; tracker-engine `1,460/1,460`, Activity `257/257`, zero failures/errors/skips | complete affected-module regression is green at `c4d97333d`; this remains host evidence and does not establish a source materializer or production query. |
| TI-B112 | three fresh read-only corrected-boundary R1 adversaries at `c4d97333d`: data/migration, Android/power/privacy, product/scope | aggregate `FAIL`; no Gradle or edits by reviewers | Accepted shared blockers: metadata-only source activation/retention loss, global live-projection head-of-line blocking, all-enabled-or-nothing source rollout, capture-coupled control reachability, and infinite terminal optional-control recovery. Accepted migration truth fixes and Steps mode split are required. Cell process-stable replay identity remains a Cell-specific activation blocker. No materializer/UI authorization. |
| TI-B113 | `:tracker:engine:testDebugUnitTest :sensor:activity:testDebugUnitTest --no-daemon --no-parallel --console=plain` after `09f32d22e` + `eb5dc1885` | `BUILD SUCCESSFUL in 6m 56s`; 263 tasks; tracker-engine `1,480/1,480`, Activity `262/262`, zero failures/errors/skips | complete affected-module host regression proves lane-backed rollout fixtures, partial source acceptance, CONTROL/capture separation, source-local Activity recovery, and terminal/transient control classification remain compatible with the existing engine and Activity contracts. |
| TI-B114 | `:core:base:testDebugUnitTest --no-daemon --no-parallel --console=plain` | `BUILD SUCCESSFUL in 38s`; 75 tasks; `849/849`, zero failures/errors/skips | complete core database regression covers the 66-entity v28 schema, source product-lane cursor/retention pin, WAL high-water activation, deletion, and preserved tracker-run evidence. |
| TI-B115 | `:app:testDebugUnitTest --tests "*DefaultTrackingStartupGateTest" --tests "*BootReceiverTest" --tests "*PostDeletionAutomaticControlRestorerTest"` | `BUILD SUCCESSFUL in 1m 38s`; 569 tasks | process startup opens after frozen-v27 authority without consulting live source poison; boot/post-deletion recovery finishes terminal disabled/contained control and retains only explicit transient retry work. |
| TI-B116 | `:core:base:connectedDebugAndroidTest "-Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test"` on `Medium_Phone(AVD) - 16` | `BUILD SUCCESSFUL in 1m 30s`; 104 tasks; `8/8`, zero skipped/failed | populated v27 facts and prior diagnostic reasons survive migration; runtime remains fenced without fabricated end time; the new product-lane table starts empty; reopen/query/delete/no-resurrection boundaries remain green. |
| TI-B117 | post-commit `checkRoomSchemaDrift --no-daemon --no-parallel --console=plain` at `eb5dc1885` | `BUILD SUCCESSFUL in 8s`; 42 tasks | committed `28.json`, Room entities/DAO, and direct unreleased-v28 migration agree; the new schema has 66 entities and no drift. |
| TI-B118 | focused Activity callback barrier, registration arbiter, receiver, and receiver thread-safety tests, followed by full `:sensor:activity:testDebugUnitTest --no-parallel --max-workers=1 '-Pksp.incremental=false'` | focused `BUILD SUCCESSFUL in 1m 1s` (207 tasks); full `BUILD SUCCESSFUL in 51s` (203 tasks); final XML `280/280`, zero failures/errors/skips | process-local permits drain after timeout/throw/cancellation; capture-closing authorization is transactionally revalidated and durably acknowledged; non-closing mutation cannot publish stale rollout authority. |
| TI-B119 | `:core:base:testDebugUnitTest --no-parallel --max-workers=1 '-Pksp.incremental=false'` | `BUILD SUCCESSFUL in 34s`; 75 tasks; final XML `855/855`, zero failures/errors/skips | duplicate/malformed lane authority fails closed, source-local retention remains isolated, and callback-barrier metadata/DAO invariants agree with the unreleased v28 schema. |
| TI-B120 | full `:tracker:engine:testDebugUnitTest --no-parallel --max-workers=1 '-Pksp.incremental=false'`, then focused `LegacyV27ProjectionRecoveryTest` after adding the complete-checkpoint/outbox-only Location case | full `BUILD SUCCESSFUL in 10m 29s` (228 tasks); focused `BUILD SUCCESSFUL in 32s` (228 tasks), `22/22` | executable-catalog admission, source-local live recovery, bounded optional-control scheduling, truthful FGS mode, retryable projection failure, and v27 Location partial preservation pass together. |
| TI-B121 | `:app:testDebugUnitTest --no-parallel --max-workers=1 '-Pksp.incremental=false'` | `BUILD SUCCESSFUL in 4m 14s`; 573 tasks; final XML `638/638`, zero failures/errors/skips | boot, startup, deletion, and post-deletion restoration agree with the bounded optional-control and callback-containment boundary. |
| TI-B122 | `:core:base:compileDebugAndroidTestKotlin`; then targeted `:core:base:connectedDebugAndroidTest` for `AppDatabaseMigration27To28Test` | compile `BUILD SUCCESSFUL in 4s` (48 tasks); connected APK assembly completed, execution failed after 104 tasks with `No connected devices!` | migration code/test compiles, but the exact `2b9265ce8` device migration remains unexecuted. Prior TI-B116 evidence is retained, not relabeled as a current-boundary pass. |
| TI-B123 | post-commit `checkRoomSchemaDrift --no-parallel --max-workers=1` at `2b9265ce8` | `BUILD SUCCESSFUL in 7s`; 42 tasks | committed `28.json`, entities, DAO queries, and direct unreleased-v28 migration are drift-free after lane and callback-barrier expansion. |
| TI-B124 | serialized current-boundary host baseline at `d51723280`: full `:tracker:engine:testDebugUnitTest`, full `:app:testDebugUnitTest`, full `:feature:dashboard:testDebugUnitTest`, and `checkRoomSchemaDrift` | engine `BUILD SUCCESSFUL in 6m 2s`, `1,544/1,544`; app `BUILD SUCCESSFUL in 1m 18s`, `647/647`; Dashboard `BUILD SUCCESSFUL in 48s`, `200/200`; schema guard `BUILD SUCCESSFUL in 7s`, all zero failures/errors/skips | proves the corrected shared spine compiles and its complete host suites agree; the Dashboard result compiled a protected user-owned untracked test but that file was not edited or staged. This is not device, source materialization, product-query, or rollout proof. |
| TI-B125 | three fresh corrected-R1 `gpt-5.6-sol` high-reasoning read-only adversaries at `d51723280`: data/migration, Android/power/privacy, and product/scope | aggregate `PASS_TO_BEGIN_MANUAL_STEPS_ONLY`; no reviewer edits or Gradle runs | no shared blocker/high requires another horizontal platform wave. TI-410 must close its executable binding/atomic receipt-cursor-delete transaction, truthful Steps state, existing UI/start surfaces, and typed export/import gates. Automatic/ambient Steps, other sources, broad DayOverview/UI, device proof, and rollout remain blocked. |
| TI-B126 | focused `ActivityCallbackRetryStoreTest`, then complete `:sensor:activity:testDebugUnitTest` after `bfa0c9da1` | focused `BUILD SUCCESSFUL in 50s`; full `BUILD SUCCESSFUL in 1m 15s`; final XML `310/310`, zero failures/errors/skips | exact interrupted `.bin.new` writes are removed before bounded capacity accounting; backups remain recoverable; undeletable/inventory/sync failure closes storage rather than opening an unbounded retry path. |
| TI-B127 | focused `PostDeletionAutomaticControlRestorerTest` plus startup tests, focused `ArchitecturalFitnessTest`, then complete `:app:testDebugUnitTest` after `648f894a4` | focused recovery/startup `BUILD SUCCESSFUL in 1m 43s`; architecture `BUILD SUCCESSFUL in 31s`; full app `BUILD SUCCESSFUL in 2m 10s`; final XML `652/652`, zero failures/errors/skips | blocked post-deletion recovery performs no permanent-state polling, retains one epoch/generation-fenced in-process obligation, and rearms only on explicit authoritative Ready; architecture boundaries and the full app graph remain green. |
| TI-B128 | `:core:base:testDebugUnitTest :tracker:engine:testDebugUnitTest --tests "*SourceProjectionStateExactCursorTest" --tests "*StepsSessionFactProjectionLaneTest" --tests "*DurableSourceEventSinkTest" --tests "*SourcePipelineRecoveryTest" --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false'` | `BUILD SUCCESSFUL in 3m 25s`; 242 tasks | dormant Steps lane verifies exact full-authority cursor CAS, atomic receipt/fact/evidence/cursor commits, exact replay, collision/poison isolation, valid-prefix preservation, retained-floor stale reconciliation, and source-local admission/startup hints. It does not activate a production writer. |
| TI-B129 | `:tracker:engine:testDebugUnitTest --tests "*StepsSessionFactProjectionLaneTest" --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false'` after strengthening negative product-time retention | `BUILD SUCCESSFUL in 3m 4s`; 232 tasks | a negative wall time remains terminal poison even when acquired time is above an advanced retention floor; malformed data cannot be relabeled stale and silently discarded. |
| TI-B130 | `:app:testDebugUnitTest --tests "*RetentionPipelineWorkerRobolectricTest" --tests "*DataRetentionWorkerTest" --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false'` | `BUILD SUCCESSFUL in 1m 47s`; 569 tasks | both retention workers lazily resolve the Steps lane after startup reconciliation, hold the exact startup-generation operation lease during drain, reconcile before WAL prune, and exercise enabled/disabled paths. |
| TI-B131 | `:core:base:testDebugUnitTest :app:testDebugUnitTest --tests "*StepFactRevisionDaoTest" --tests "*RetentionPipelineWorkerRobolectricTest" --tests "*DataRetentionWorkerTest"`, followed by the final worker rerun | combined `BUILD SUCCESSFUL in 4m 2s` (587 tasks); final worker rerun `BUILD SUCCESSFUL in 1m 39s` (569 tasks) | expired self-contained Steps UPSERT payloads are removed at the authoritative monotonic retained-from floor in both real worker paths; exact-end overlap remains; redacted RETRACT tombstones survive; stale WAL/failure reconciliation still precedes prune. |
| TI-B132 | fresh source-lane/retention adversarial sequence around `8bb6d606f` and `af846143c` | reviewers found and corrections mitigated destination-time resurrection, stale terminal WAL pinning, negative-time misclassification, valid-prefix rollback, retention liveness, eager database resolution/startup-generation race, and retained fact payload; different final reviewer reported no remaining `BLOCKER`, `HIGH`, or `MEDIUM` | acceptance is limited to the dormant Steps writer and its retention integration. Production executable catalog, canonical activation, legacy writer fence, product reader/UI, automatic/ambient Steps, and other source materializers remain absent. |
| TI-B133 | `:core:base:testDebugUnitTest --tests '*SourceDestinationOwnerDaoTest' --tests '*StepFactRevisionDaoTest' --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false'` | `BUILD SUCCESSFUL in 50s`; 75 tasks | exact owner/generation checks, monotonic ABA-safe low-level CAS, and service-run-stamped fact validation pass; the CAS is not a cutover coordinator. |
| TI-B134 | seven focused `:tracker:engine:testDebugUnitTest` bridge/fence/recovery classes with serialized Gradle flags | `BUILD SUCCESSFUL in 1m 19s`; 228 tasks | paired logical/run identity reaches `SessionSegment`; covered-zero Steps-only segments survive; legacy and candidate writes require their exact owner generation; projection/recovery fixtures remain green. |
| TI-B135 | `:app:testDebugUnitTest --tests '*RetentionPipelineWorkerRobolectricTest' --tests '*DataRetentionWorkerTest'` | `BUILD SUCCESSFUL in 2m 26s`; 569 tasks | app graph resolves only the owner DAO required by write fencing; retained fact pruning/deletion behavior remains green and the permanent legacy owner fence survives collected-data deletion. |
| TI-B136 | `:core:base:connectedDebugAndroidTest '-Pandroid.testInstrumentationRunnerArguments.class=com.adsamcik.tracker.shared.base.database.AppDatabaseMigration27To28Test'` on `Medium_Phone(AVD) - 16` | `BUILD SUCCESSFUL in 1m 1s`; `8/8`; 104 tasks | exact current v27→v28 migration adds nullable durable session/run bridge, service-run fact identity, and legacy owner generation 1 while preserving production reopen/query/delete/no-resurrection behavior. |
| TI-B137 | three independent high-reasoning R1 perspectives on the pre-`ef1da62d2` diff: data/migration, Android/power/privacy, product/scope | aggregate `NO_GO` for candidate activation and Trip Detail wiring; read-only | accepted blockers/highs: global-owner historical reinterpretation, cross-run completeness leakage, unusable rollback generation, mixed-batch legacy failure, post-candidate deletion reconstruction, retention/deletion truth, and non-converging product state. Premature production query/UI and query-only helpers were removed; dormant identity/fence foundation accepted as proportionate and battery-neutral. |
| TI-B138 | post-commit `checkRoomSchemaDrift --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false'` at `ef1da62d2` | `BUILD SUCCESSFUL in 33s`; 42 tasks | committed entities, DAO, direct unreleased-v28 migration, and `28.json` agree after the durable service-run/owner-fence slice. |
| TI-B139 | two additional fresh R1 correction adversaries: data/migration and Android/lifecycle/power | initial aggregate `NO_GO`; read-only | both independently found that a provider-owning cancelled `APPLYING` action was excluded from exact retirement; the data review also found accepted automatic replay could finalize a live `START_ACCEPTED` provider. A third fresh correction thread was unavailable under the four-thread ceiling, so this is not represented as a fresh three-review round; the required earlier three-perspective R1 remains TI-B125. |
| TI-B140 | focused `:tracker:engine:testDebugUnitTest --tests "*AuthoritativeSessionCoordinatorTest" --tests "*TrackerServiceSourceSessionTest" --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --no-configuration-cache` | `BUILD SUCCESSFUL in 1m 21s`; coordinator `52/52`, service session `19/19`, combined `71/71`; zero failures/errors/skips | post-publication start cancellation uses its exact `APPLYING` claim, incomplete removal remains cleanup-pending and retries, reconfigure retires the newest claim, automatic `APPLYING`/`START_ACCEPTED` replays cannot finalize the old run, and cancelled service operations force full stop rather than suspension. |
| TI-B141 | split source runtime/broker/registration shard and finalizer/shutdown shard with serialized Gradle flags | runtime shard `BUILD SUCCESSFUL in 1m 1s`; lifecycle shard `BUILD SUCCESSFUL in 37s` | all six runtime claim/ack paths, shared registration isolation, finalizers, and teardown sequencing pass. A preceding oversized combined shard ended in Gradle test-results `java.io.EOFException` without an assertion report; split and complete reruns distinguish it from a code failure. |
| TI-B142 | complete `:tracker:engine:testDebugUnitTest --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --no-configuration-cache` | `BUILD SUCCESSFUL in 6m 52s`; `1,640/1,640`, zero failures/errors/skips | complete engine regression agrees with exact service-run manifests, provider claims, strict retirement evidence, stale automatic fencing, run-scoped recovery, and existing source/product containment. It does not prove device provider behavior or authorize a writer. |
| TI-B143 | `:core:base:testDebugUnitTest :tracker:api:testDebugUnitTest --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --no-configuration-cache` | `BUILD SUCCESSFUL in 24s`; core `870/870`, tracker API `65/65`, zero failures/errors/skips | full core v28/DAO/integrity regression and runtime-stop dispatcher delivery/defer/release contracts pass. The first attempt used the directory name `:tracker:api-module` and failed task selection before tests; the registered project path was verified in `settings.gradle.kts` and corrected to `:tracker:api`. |
| TI-B144 | exact `AppDatabaseMigration27To28Test` via `:core:base:connectedDebugAndroidTest` on `Medium_Phone(AVD) - 16` | `BUILD SUCCESSFUL in 34s`; `8/8`; 108 tasks | the direct unreleased-v28 migration preserves populated v27 facts, adds exact manifest/run/action authority, production-reopens/queries, deletes, and does not resurrect data. |
| TI-B145 | `:app:assembleDebug`, then post-commit `checkRoomSchemaDrift` | assemble `BUILD SUCCESSFUL in 1m 9s` (654 tasks); schema guard `BUILD SUCCESSFUL in 19s` (46 tasks) | the complete debug app graph compiles with the exact lifecycle boundary, and committed `28.json`, entities, DAO, and direct v27→v28 migration agree. Existing Moshi Kapt deprecation is unchanged build output, not a changed-source failure. |
| TI-B146 | same-reviewer correction verification after the accepted fixes | data and Android/service scopes both `GO`; read-only | no remaining `BLOCKER`/`HIGH` in the corrected exact-claim boundary. This is transparently a correction verification by the reviewers who found the defects, not a fresh certification and not materializer, UI, ambient, or rollout approval. |
| TI-B147 | scoped local commits `ca5b1ffad` and `47fb0d842` | 47-file durable run/provider ownership commit plus 5-file teardown delivery/retry commit; staged checks clean | commits contain only the verified core/tracker files. User-owned import/export changes, statistics line-ending artifact, untracked Dashboard test, DOCX, and orchestrator prompt remain unmodified and unstaged. |
| TI-B148 | focused writer-provenance/recovery and DAO gate after the final corrections | writer/recovery `48/48`; final DAO plus writer/recovery rerun `BUILD SUCCESSFUL in 1m 7s`; zero failures/errors/skips | exact event-manifest selection, control/consent rejection, ambiguous retry, current/stale/candidate ownership, missing-authority retry, mixed-source quarantine, acquisition clock, migrated legacy replay-once, and processor session identity pass together. |
| TI-B149 | complete serialized `:tracker:engine:testDebugUnitTest :core:base:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug` with configuration cache disabled and one worker | `BUILD SUCCESSFUL in 9m 17s`; 716 tasks; engine `1,649/1,649`, core `872/872`, app `654/654`, zero failures/errors/skips | complete affected host regressions and the debug APK graph agree with queued writer provenance, quarantine retention, and tier-reactivation identity. This is not device/provider, production query, or rollout proof. |
| TI-B150 | exact `AppDatabaseMigration27To28Test` via `:core:base:connectedDebugAndroidTest` on `Medium_Phone(AVD) - 16` | `BUILD SUCCESSFUL in 55s`; `8/8`, zero skipped/failed | direct unreleased-v28 migration stamps released pending commands as legacy generation 1, adds fail-closed quarantine acquisition time, preserves production reopen/query/delete/no-resurrection, and remains data-compatible with released v27. |
| TI-B151 | post-commit `checkRoomSchemaDrift --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --no-configuration-cache` at `30051416d` | `BUILD SUCCESSFUL in 20s`; 46 tasks | committed entities, DAOs, direct v27→v28 migration, and generated `28.json` agree. The expected pre-commit run failed only because the intentionally regenerated schema was uncommitted. |
| TI-B152 | three independent focused correction reviews: exact manifest tests, quarantine privacy/retention, and full writer-fence diff | final scoped verdict `GO`; no unresolved `BLOCKER`/`HIGH` | reviews found and corrections mitigated policy-tier session ID loss, missing-owner silent acknowledgement, malformed-row recovery overclaim, acquisition-time retention, and missing migrated-valid replay evidence. This is scoped approval of `30051416d`, not TI-VS28 cutover, product UI, ambient, or rollout certification. |
| TI-B153 | scoped local commit `30051416d` | 22 verified tracking/schema/test files; cached diff check clean | user-owned import/export changes, statistics line-ending artifact, untracked Dashboard test, DOCX, and orchestrator prompt were excluded and remain unstaged. |
| TI-B154 | three independent focused read-only audits before the selector/cutover wave: historical selector, cutover coordinator, and consumer truth | converged on the same smallest design; no reviewer edits | use existing `SessionSegment.serviceRunId`, immutable manifests, exact product lane and run completeness; never use current ownership as history; never switch mid-run; add no table, generic history platform or UI before a typed observable state can propagate completeness to every numeric consumer. |
| TI-B155 | `:core:base:testDebugUnitTest --tests '*StepFactRevisionDaoTest' --tests '*SourceProjectionStateExactCursorTest' :stats:data:testDebugUnitTest --tests '*StepsSegmentHistorySelectorTest'` with serialized Gradle flags | `BUILD SUCCESSFUL in 1m 35s`; `33/33`, zero failures/errors/skips: DAO `9/9`, exact lane `3/3`, selector `21/21` | immutable run writer selection, current-owner independence, zero/baseline/partial/deleted/materializing states, correction reattribution, retained floor, epoch fencing, overflow, active/retired/cutoff and malformed-lane behavior pass. |
| TI-B156 | complete serialized `:core:base:testDebugUnitTest :stats:data:testDebugUnitTest :tracker:engine:testDebugUnitTest :app:assembleDebug` with configuration cache disabled and one worker | `BUILD SUCCESSFUL in 8m 46s`; 720 tasks; core `876/876`, stats data `139/139`, engine `1,649/1,649`, zero failures/errors/skips | complete affected host regressions and debug APK graph agree with the dormant selector. This is not a production consumer, device/provider, automatic/ambient, or rollout proof. |
| TI-B157 | post-commit `checkRoomSchemaDrift` and scoped local commit `b20e1efaa` | schema guard `BUILD SUCCESSFUL in 30s`, 46 tasks; six-file commit; cached diff check clean | `28.json` is unchanged; only two DAOs, two DAO tests, the internal selector and its tests were committed. User-owned import/export changes, statistics line-ending artifact, untracked Dashboard test, DOCX, and orchestrator prompt remain unstaged. |
| TI-B158 | three independent adversarial reviews of the atomic Steps transition before reconciliation integration | data/DAO: no remaining `BLOCKER`/`HIGH`/`MEDIUM`; lifecycle: `HIGH` mitigated; app/deletion: `MEDIUM` mitigated with one `MEDIUM` integration-test gap retained | verified atomic activation/rollback/deletion generations and receipt/cursor behavior; fixed pending-signal half-pair validation, exact destination-owner authority in rollout/admission, and residual legacy Steps deletion. The remaining production deletion-service→database→closed-barrier→re-arm seam is explicitly unproven. No reviewer authorized product query, device behavior, source activation, or rollout. |
| TI-B159 | reconciliation merge `9b8ab4b43e0d5ca2e729f511b48936b0dbdfbb6a` and exact `ciCheck --continue --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --console=plain --no-configuration-cache` | `BUILD SUCCESSFUL in 21m 46s`; 1,991 actionable tasks, 224 executed, 1,767 up-to-date | includes Room drift, `ciArchitectureCheck`, release lint, `ciUnitTest`, Detekt, release SQLite runtime/linkage, 27 release-evidence tests, and 16 reviewed dependency-metadata components. This is repository host/build-quality proof, not provider/process/reboot/FGS, migration rerun, production-query/UI, device-energy/OEM, source-scenario, or rollout proof. Graph and continuation commands are in `CONTINUATION_HANDOVER.md`. |
| TI-B160 | targeted `:app:connectedDebugAndroidTest` for `CollectedDataDeletionStepsRearmIntegrationTest` on `Medium_Phone(AVD) - 16` / API 36, twice consecutively on the exact source tree committed as `3b2365547`, then once after the required no-op rebase | pre-handover runs `BUILD SUCCESSFUL in 1m 6s` and `47s`; post-rebase run `BUILD SUCCESSFUL in 48s`; each ran `1/1` test and 714 actionable tasks; latest XML has zero failures/errors/skips | The production deletion provider uses the singleton lifecycle/coordinator and default `AppDatabase` deletion; post-rearm diagnostics failure retains the durable marker and closed barrier, and retry repeats deletion, advances fresh generations, reconstructs the empty manual-capture Steps lane at retained WAL high-water, reopens matching startup authority, and leaves startup Ready. This closes only the same-process deletion-service→database→closed-barrier→Steps-rearm seam; it is not production query/export, provider/cold-process/reboot, full connected-suite, activation, or rollout evidence. |
| TI-B161 | three fresh `gpt-5.6-sol` high-reasoning read-only adversaries plus focused app/engine/static verification | two Android `HIGH` findings mitigated; no unresolved scoped `BLOCKER`/`HIGH`; engine `BUILD SUCCESSFUL in 3m 11s` (234 tasks), app deletion suite `BUILD SUCCESSFUL in 52s` (575 tasks), Detekt `BUILD SUCCESSFUL in 33s` and post-rebase in `18s` (5 tasks) | setup no longer destroys pending deletion authority, retained canonical state is rerunnable, and transient readiness is not asserted. Existing stale-ingress/WAL/projection/recovery suites supply the data-resurrection evidence intentionally kept out of the app seam. Cold-process recovery, installed-database suite isolation, product query/export/UI, provider behavior and rollout remain open. |
| TI-B162 | three independent focused history-contract adversaries, final read-only diff audit, focused stats tests, stats lint/Hilt compile, Detekt, `ciUnitTest`, and `ciCheck --continue --no-parallel` | all scoped `BLOCKER`/`HIGH` findings mitigated; exact committed-code `ciCheck` `BUILD SUCCESSFUL in 8m 32s` (1,991 tasks: 662 executed, 297 from cache, 1,032 up-to-date); focused gate `BUILD SUCCESSFUL in 49s` (219 tasks); `ciUnitTest` `BUILD SUCCESSFUL in 12m 18s` (990 tasks); lint/Hilt `BUILD SUCCESSFUL in 40s` (569 tasks); Detekt `BUILD SUCCESSFUL in 30s` | explicit policy is required before reporting disabled; baseline, covered zero, positive recording, partial candidate lag, and unknown legacy coverage remain distinct. The Room observer is restricted to one selected session and has no production consumer, provider/writer mutation, day/list fan-out, export, UI, activation, device, or rollout claim. Profile/batch before multi-row use. |
| TI-B163 | three fresh R1 `gpt-5.6-sol` high-reasoning read-only adversaries: data/replay/migration, Android lifecycle/privacy/power, and product/scope | aggregate `NO_GO` for the attempted production-wired selected-trip deletion; the unsafe `DefaultTripRepository` delegation and presentation-owned Steps retraction writer were withdrawn before commit | shared `BLOCKER`/`HIGH` cases were legacy generation-1 bypass, finalization-versus-writer-teardown resurrection, unchecked active/`STOPPING` failure, stale `daily_summary`, retention/import/future-writer bypass, absent automatic cursor wake, and misleading permanent-deletion scope. These findings block production deletion and consumer wiring; they do not block a dormant battery-neutral fence primitive. |
| TI-B164 | focused compile and tests for `03bbda2f1`: `:core:base`, `:tracker:engine`, and `:stats:data` Kotlin compile; fence/Steps DAO, candidate lane, selected-session selector/observer; Detekt and app Hilt; Android-test Kotlin compile | production compile `BUILD SUCCESSFUL in 2m 17s` (136 tasks); XML green at fence DAO `3/3`, Steps fact DAO `9/9`, candidate lane `14/14`, selected-session selector/observer `27/27`; Detekt/app Hilt `BUILD SUCCESSFUL in 2m 38s` (366 tasks); Android-test Kotlin compilation completed | one oversized combined test run ended in Gradle test-results `EOFException`, followed by a Windows `classes.jar` lock; after stopping two stale daemons, every affected class passed in isolated serialized runs. No assertion failure is attributed to the environment event. Connected migration/device execution was not rerun for this schema revision. |
| TI-B165 | code commit `03bbda2f1` plus post-commit `checkRoomSchemaDrift --no-daemon --no-parallel --max-workers=1 '-Pksp.incremental=false' --console=plain --no-configuration-cache` | `BUILD SUCCESSFUL in 22s`; 46 tasks | committed `28.json`, the 69 registered entities, DAO, and direct unreleased-v28 migration agree. This proves schema drift only, not production deletion, import/export, device migration, `QUERYABLE`, UI, activation, or rollout. |
| TI-B166 | cleanup containment commit `88309387d`: focused `SessionSegmentDaoTest`, `DatabaseMaintenanceWorkerTest`, `DefaultCollectedDataWriterQuiescerTest`, complete nested `ArchitecturalFitnessTest`, root Detekt, and lifecycle/cleanup/final-diff reviews | core `BUILD SUCCESSFUL in 1m 13s` (79 tasks; XML `11/11`); app `BUILD SUCCESSFUL in 1m 9s` (575 tasks; worker `3/3`, quiescer `3/3`, architecture 37 assertions); Detekt `BUILD SUCCESSFUL in 34s` (5 tasks); zero failures/errors/skips | the first combined run collided with another shared-worktree Gradle process and ended in test-results `EOFException`, so it is discarded rather than counted. Evidence proves database-mutation retirement, inert persisted-worker compatibility, UI cancellation request/deletion-awaited cancellation, placeholder preservation, and initial named query filters. It does not prove completion of every asynchronous cancellation, all-DAO exclusion, durable ownership/acknowledgement, orphan reclamation, device execution, full current-tree `ciUnitTest`/`ciCheck`, product deletion, `QUERYABLE`, activation, or rollout. |
| TI-B167 | remaining zero-sample read correction `24b9aeffb`, focused Room rerun, corrected Detekt, evidence review, and independent next-wave scope/document-falsifier reviews | core `BUILD SUCCESSFUL in 1m 5s` (79 tasks; XML `12/12`, zero failures/errors/skips); Detekt `BUILD SUCCESSFUL in 39s` (5 tasks); clean two-file commit; final falsifier's wording findings corrected before the handover commit | `24b9aeffb` adds positive-sample predicates to app-age/hour/night/dawn and ActivityRecognition reads. Combined with `88309387d`, every zero-sample row is excluded from those and the named daily/live/source/all-time activity queries; other DAO reads remain unchanged. Four `HIGH` and three `MEDIUM` next-wave findings are mitigated by TI-D111; no production consumer, deletion, only-Steps product query, device/provider behavior, activation, or rollout is proven. |
| TI-B168 | selected Trip Detail consumer commit `f9b1c3c45`; three independent product/Android/validation adversaries, one fresh corrected-diff reviewer, four focused JVM/Compose suites, root Detekt, and statistics lint | focused `BUILD SUCCESSFUL in 36s` (194 tasks; XML `33/33`: mapping `10/10`, presenter `9/9`, ViewModel `6/6`, Compose `8/8`); Detekt `BUILD SUCCESSFUL in 18s`; `:feature:statistics:lintDebug` `BUILD SUCCESSFUL in 32s` (338 tasks), no new issue | every commit-local `BLOCKER`/`HIGH` was mitigated by removing Trip Detail's presentation-only delete and `sampleCount` Location inference, isolating history failure, adding explicit retry, emitting immediate materializing state, and tying observation/optional reads to screen lifecycle. The validation adversary's separate `HIGH` remains accepted: `sample_count > 0` list filtering still blocks ordinary source-only discovery and `QUERYABLE`. The module lint baseline filters 137 errors/32 warnings and new copy is English fallback. List/day/live composition, other delete paths, exact capture/Location evidence, numeric consumers, `ciUnitTest`/`ciCheck`, device/provider/visual/accessibility/energy proof, activation, and rollout remain unverified. |
| TI-B169 | exact service-run presentation settlement commit `9aeb8853a`; independent schema, lifecycle, and segment/product adversaries; focused engine/core/API tests; migration-test compile; Detekt; affected-module lint; committed Room drift | engine lifecycle/component/store/orchestrator/crash suite `BUILD SUCCESSFUL in 4m 20s` (230 tasks); focused core DAO `BUILD SUCCESSFUL in 18s`; tracker API descriptor `BUILD SUCCESSFUL in 9s`; root Detekt `BUILD SUCCESSFUL in 13s`; lint `BUILD SUCCESSFUL in 2m 19s` (383 tasks, no new issue); post-commit schema guard `BUILD SUCCESSFUL in 8s` (46 tasks) | one physical run owns one exact Room-bound segment; replacement runs create separate segments under one logical ID; DataStore exact CAS cannot overwrite stop state; terminal/idempotent quiescence follows writer shutdown and never implies empty/materialized/retained/deletable. No device is attached, so the compiling v27→v28 migration test was not executed. All-source evidence evaluation, cleanup, logical-entry grouping, Steps reverse binding, ordinary discovery, materialization, `QUERYABLE`, provider/device/energy proof, activation, and rollout remain blocked. |
| TI-B170 | authoritative host aggregate after `9aeb8853a`, stale-test correction `d067704a9`, focused rerun, and corrected aggregate rerun | first `ciUnitTest --no-parallel` failed in `10m 25s` after 1,724 tracker-engine tests because `MultiSessionLifecycleTest` still required deletion of a zero-cycle row; no production assertion failed. Corrected focused class `BUILD SUCCESSFUL in 2m 7s` (234 tasks). Corrected `ciUnitTest --no-parallel` `BUILD SUCCESSFUL in 7m 2s` (990 tasks: 6 executed, 984 up-to-date) | the broad suite now agrees that absence of source-neutral cycles cannot prove absence of Wi-Fi/Cell facts. The initial failure is retained as regression evidence, not hidden. This is authoritative host-test evidence only; full `ciCheck`, connected migration, provider/process/reboot/device/energy, production-query, activation, and rollout remain unverified. |
| TI-B171 | reverse-binding commit `f2c7a3225`; focused selector/mapping tests; complete stats API/data tests; root Detekt; stats-data lint; Room schema guard; two final read-only adversaries | focused `BUILD SUCCESSFUL in 43s` (209 tasks; `38/38`); full affected contract `BUILD SUCCESSFUL in 46s` (219 tasks), stats data `156/156`, stats API JVM `308/308`, Android host `308/308`; static/schema gate `BUILD SUCCESSFUL in 1m 18s` (345 tasks), no new lint issue; zero failures/errors/skips and no remaining scoped `BLOCKER`/`HIGH`/`MEDIUM` | new-v28 selected Steps history accepts only the exact run-owned segment; null/foreign bindings fail before historical evidence selection, and migrated ownership remains typed `LEGACY_UNVERIFIED`. This is host read-contract/schema-parity evidence only; it does not prove ordinary discovery, logical-entry grouping, all-source reclamation, deletion, `QUERYABLE`, device/provider/process behavior, activation, or rollout. |
| TI-B172 | exact capture/qualified-history commit `c1d6a4a62`; focused 42-case selector suite; complete core/stats API/data tests; root Detekt; core/stats-data lint; Room schema guard; three corrected-diff reviews plus final post-extraction read review | focused `BUILD SUCCESSFUL in 1m 12s` (209 tasks; `42/42`); full affected `BUILD SUCCESSFUL in 1m 26s` (233 tasks), core `896/896`, stats data `168/168`, stats API JVM `308/308`, Android host `308/308`; Detekt `BUILD SUCCESSFUL in 32s` (5 tasks); lint/schema `BUILD SUCCESSFUL in 2m 16s` (347 tasks), no new lint issue or schema change; zero failures/errors/skips and no remaining scoped `BLOCKER`/`HIGH` | exact revisioned capture intent plus source-local available covered/recorded evidence qualifies Steps without `sampleCount`; control-only, stale-epoch, fenced, retracted, failed, malformed, and unknown-purpose cases fail closed; null/null and legacy-unverifiable compatibility rows qualify no source; keyset traversal crosses rejected 64-row batches and correction ownership stays with the exact physical run. The initial nonexistent module-Detekt task selection and one post-extraction missing-import compile were corrected before the passing gates. This foundational batch selector has no production list caller, so logical-entry grouping, ordinary navigation, deletion, `QUERYABLE`, device/provider/process behavior, activation, and rollout remain unverified. |
| TI-B173 | logical-entry grouping commit `923bf2025`; focused 53-case Steps history suite; complete core-base/stats-data tests; root Detekt; affected lint; Room schema guard; three final corrected-diff reviews | focused `BUILD SUCCESSFUL in 1m 19s` (209 tasks; `53/53`); full affected `BUILD SUCCESSFUL in 1m 26s` (223 tasks), core `896/896`, stats data `179/179`; Detekt `BUILD SUCCESSFUL in 35s` (5 tasks); lint/schema `BUILD SUCCESSFUL in 1m 55s` (347 tasks), no new lint issue or schema change; zero failures/errors/skips and no remaining scoped `BLOCKER`/`HIGH`/`MEDIUM` | evidence-bearing members discover identities; the newest exact or explicitly attributed migrated sibling determines logical-entry recency; grouping precedes limits; 65 physical members cross the sibling page boundary with exact per-run truth; invalid reverse links are excluded; migrated members remain typed/unqualified; null/null legacy remains physical; no cross-run number is invented. Earlier reviews caught and the final code corrected synthetic max tuples, unbounded sibling materialization, migrated-row splitting, weak page-two assertions, and coarse-seed-only recency. The reader is internal and has no public list/UI caller, so ordinary navigation, deletion, `QUERYABLE`, device/provider/process behavior, activation, and rollout remain unverified. |
| TI-B174 | public Steps-only history read commit `90f7e758e`; combined stats API/data/statistics tests, root Detekt, stats-data lint, app Hilt compile, post-commit Room drift, and three final corrected-diff reviews | combined command `BUILD SUCCESSFUL in 41s` (672 tasks: 9 executed, 663 up-to-date); stats API JVM `313/313`, Android host `313/313`, stats data `183/183`, feature statistics `427/427`, zero failures/errors/skips; schema guard `BUILD SUCCESSFUL in 2s` (42 tasks); no remaining scoped `BLOCKER`/`HIGH`/`MEDIUM` | exact revisioned capture/control and qualified-source authority are public and consistent; any mixed-source revision or unverifiable physical member fails the Steps-only predicate closed; predicate application precedes limits; replacement rows expose one opaque nonnumeric entry and react to a source-local fence. Review withdrew an unsafe independently limited suppression-ID API, so physical Trip suppression remains blocked until a concrete consumer supplies its bounded candidate window. No UI/navigation, cross-run total, selected deletion, `QUERYABLE`, device/provider/process/energy proof, activation, or rollout is claimed. |
| TI-B175 | candidate-scoped page commit `2ac4e399d`; complete Stats API/data tests, focused selector rerun, root Detekt, Stats lint, app Hilt compile, and two independent final read-only reviews | full affected tests `BUILD SUCCESSFUL in 28s` (215 tasks): Stats API JVM `314/314`, Android host `314/314`, stats data `191/191`; focused selector command `BUILD SUCCESSFUL in 24s` (219 tasks); zero failures/errors/skips and no remaining scoped `BLOCKER`/`HIGH`/`MEDIUM`. The shared static command completed Detekt, Stats lint with no new issue, and Hilt compile before later failing only on Dashboard translations outside this commit. | one transaction snapshots at most 100 caller candidates, suppresses whole exact Steps-only intent groups independently of current qualification, emits only qualified opaque logical replacements, orders by newest authoritative physical member, and applies the final limit after merge. Physical IDs cannot escape the caller set and logical rows grant no detail/delete authority. No production list caller, ordinary navigation, `QUERYABLE`, device/provider/process/energy proof, activation, or rollout is claimed. |
| TI-B176 | live Dashboard commit `4803d8af3`; complete module tests, root Detekt, Dashboard lint, app Hilt compile, 27-locale resource validation, two post-correction focused shards, and independent corrected-diff review | combined gate `BUILD SUCCESSFUL in 1m 37s` (766 tasks), Dashboard `200/200` across 49 suites; focused six-class shard `BUILD SUCCESSFUL in 35s` (268 tasks), `24/24`; final stale-authority shard plus Detekt `BUILD SUCCESSFUL in 41s` (269 tasks), `14/14`; zero failures/errors/skips. Lint has zero errors and one pre-existing guarded `InlinedApi` warning; no final scoped `BLOCKER`/`HIGH`/`MEDIUM`. | exact capture authority selects a segment-bound Steps-only live surface; control Activity stays separate; replacement/stop cancels stale observation; missing/error/mismatch fails closed; only complete covered zero is numeric zero; unavailable takes precedence; Location/map/detail controls and raw milestones are absent; Stop remains reachable without Location permission; Standard milestones retain exact current-segment behavior. This is host/resource proof only, not ordinary history discovery, `QUERYABLE`, sensor/listener/device/process/battery/visual/accessibility proof, activation, or rollout. |
| TI-B177 | deterministic recent-query commit `123c6399e`; both `TripDao` variants plus equal-start/limit tests | included in the post-review combined gate below; `TripDaoTest` `16/16`, zero failures/errors/skips; cached diff check clean | suspend and Flow recent candidates now order by `(startTimeMs DESC, id DESC)`, including deterministic selection at the limit boundary. Other start-only DAO queries are unchanged. |
| TI-B178 | bounded Dashboard history commit `ab760d234`; bounded 20-candidate/5-result repository/ViewModel/route/card tests, root Detekt, Dashboard lint, 28-resource-set validation, storage review, corrected product/lifecycle review | post-review combined command `BUILD SUCCESSFUL in 4m 21s` (610 tasks: 46 executed, 564 up-to-date); selected XML `56/56` (`16/16` DAO, `40/40` Dashboard), zero failures/errors/skips; lint zero errors and one previously recorded guarded `InlinedApi` warning; all nine affected keys occur exactly once in every resource set; no remaining scoped review finding | one immutable generation of at most 20 physical rows feeds one cancelable Stats page limited to five; physical echoes cannot escape that generation; failure has no raw fallback/backfill. A top opaque Steps row blocks Last Session/map/detail, is nonclickable, and exposes no Steps total or distance; physical behavior survives only under composed authority. Replay expires after lifecycle stop so stale Content cannot restore actions. This proves bounded Dashboard `QUERYABLE` for qualified durable stopped Steps-only data on host, not the real provider/WAL/listener device scenario, broader consumers, deletion/export, activation, or rollout. |
| TI-B179 | synthetic Steps WAL/materialization boundary commit `dff1b85fd`; focused boundary, full production ingress and canonical Steps projection suites; root Detekt; adversarial corrected-diff review | combined command `BUILD SUCCESSFUL in 3m 16s` (234 tasks: 7 executed, 227 up-to-date); XML `65/65` (`2/2` boundary, `49/49` ingress, `14/14` projection), zero failures/errors/skips; Detekt `BUILD SUCCESSFUL in 21s`; final re-review clean | producer-shaped baseline and covered-zero rows do not yield the test-local positive verdict; a contiguous positive covered row is visible before any candidate receipt/cursor and materializes once under the exact candidate writer after drain; valid-prefix corruption becomes typed unverifiable; the real prefixed legacy destination remains empty. Rows are synthetic and inserted after the admission boundary, so this is not production admission/freshness, provider, normal recovery, device UI, listener-removal, process, battery, activation, or rollout proof. |
| TI-B180 | disposable manual Steps-only device-gate harness commit `1bb9749af`; Android-test Kotlin compile, root Detekt, app lint, `adb devices -l`, and two adversarial staged-diff passes | `:app:compileDebugAndroidTestKotlin` `BUILD SUCCESSFUL in 26s` (525 tasks: 5 executed, 520 up-to-date); Detekt `BUILD SUCCESSFUL in 26s` (1 task); `:app:lintDebug` `BUILD SUCCESSFUL in 56s` (1,073 tasks: 7 executed, 1,066 up-to-date); ADB returned no device rows; final review found no remaining commit-blocking defect | the harness uses a cleared disposable install, exact Steps-only policy, production automatic-control reconciliation, test-only candidate activation, production start/provider/admission/normal writer/query/stop APIs, complete durable source-set audit, and non-cancellable failure cleanup. The connected class command was not run. This is compile/static contract evidence only: no real callback, provider-to-query execution, rendered UI/accessibility, physical listener removal, process/reboot/FGS/battery/OEM, ordinary activation, or rollout is proven. |
| TI-B181 | exact continuation convergence at `4b25d39e2`: clean ancestry/non-overlap checks, authoritative host aggregate, full repository quality gate, required rebase check, local fast-forward, and protected-root SHA-256 comparison | `ciUnitTest` `BUILD SUCCESSFUL in 17m 42s` (990 tasks: 61 executed, 929 up-to-date); `ciCheck --continue` `BUILD SUCCESSFUL in 11m 17s` (1,987 tasks: 231 executed, 10 from cache, 1,746 up-to-date); `git rebase dev/v10` up to date; `git merge --ff-only` advanced local `dev/v10` from `ffd5d372f` to `4b25d39e2`; all six protected hashes unchanged | this proves the committed continuation is locally integrated and repository host/static/release gates pass. It does not execute the connected manual Steps class or prove provider, listener removal, rendered UI/accessibility, process death, reboot, FGS, battery, OEM behavior, ordinary activation, rollout, push, or release. TI-D123 authorizes only disjoint contained development while those evidence rows remain blocked. |
| TI-B182 | Pressure capability-normalized request commit `afcb009e1`; fresh corrected-diff review; forced focused provider/runtime/window/floor rerun; affected Detekt and lint; rebase and local fast-forward | focused `:tracker:engine:testDebugUnitTest --rerun-tasks` `BUILD SUCCESSFUL in 5m 3s` (230 tasks executed), XML `53/53` with zero failures/errors/skips; combined Detekt and `:tracker:engine:lintDebug` `BUILD SUCCESSFUL in 31s` (425 tasks), no new lint issue; fresh review found no blocker/high/medium | the immutable effective sample-period/report-latency tuple is normalized against real min/max delay and FIFO capability, reused for start/refresh/replacement/resume, and unsupported quality is degraded instead of renamed. Equivalent tuples do not restart and no-FIFO mode cannot claim batching. This is JVM/mock-sensor evidence only; real capability reporting, realized cadence/FIFO/flush/wakeup/energy, process/reboot, product query/UI, deletion/export, activation, rollout, push, and release remain unverified. |
| TI-B183 | Cell atomic delivery-admission commit `862e839eb`; fresh corrected-diff review; independent forced ordinal/replay rerun; full focused post-rebase runtime/Room suite; affected Detekt and lint; local fast-forward | independent exact cases `BUILD SUCCESSFUL in 4m 6s` (234 tasks, `2/2`); post-rebase focused suite `BUILD SUCCESSFUL in 3m 50s` (230 tasks, `38/38`); Detekt and `:tracker:engine:lintDebug` `BUILD SUCCESSFUL in 2m 29s` (374 tasks), no new issue and six baseline-filtered warnings; clean reviewed scope | one fresh nonempty timestamp-qualified minimized callback forms a replay-stable delivery; Room allocates only one source sequence and one WAL row across runtime-generation envelopes; prerequisite/cutoff retry fences and privacy minimization hold; a later duplicate of an older ordinal advances callback resolution without regressing the physical-run stop boundary. This is JVM/Robolectric/in-memory-Room evidence only; real Telephony callbacks, process death, radio timing, active refresh, energy/OEM behavior, product materialization/query/UI, deletion/export, activation, rollout, push, and release remain unverified. |
| TI-B184 | Pressure atomic delivery-admission commit `3a3bcbadb`; fresh NO_GO correction; independent corrected re-review; complete focused post-rebase DAO/ingress/runtime/window suite; Detekt, affected lint, Room drift, and local fast-forward | independent exact DAO/Pressure Room rerun `BUILD SUCCESSFUL in 2m 21s` (`34/34`); post-rebase focused set `BUILD SUCCESSFUL in 2m 53s` (248 tasks, `153/153`); Detekt, core/tracker lint, and schema drift `BUILD SUCCESSFUL in 1m 53s` (388 tasks), no new lint issue; clean reviewed scope | Room atomically allocates one Pressure sequence/WAL row and optional checkpoint. Duplicate checkpoint repair requires exact retained provider/auth authority over both intrinsic endpoints; retention, generation mismatch, interval narrowing, authorization split, identity collision, and rollback fail without mutation. Stable identity excludes callback-local sequences while payload checksum preserves collision detection. This is host/Robolectric/in-memory-Room evidence only; physical sensor/FIFO/flush/cadence, process death, battery, product fact/query/UI, deletion/export, activation, rollout, push, and release remain unverified. |
| TI-B185 | protected Location observation-floor commit `a4caa9f12`; corrected ordinal high-water regression; author and independent focused gates; Detekt, affected lint, clean local fast-forward | author focused set `58/58`; independent six-suite rerun `BUILD SUCCESSFUL in 1m 22s` (234 tasks), XML `58/58`, zero failures/errors/skips; Detekt and affected lint pass with no new issue; fresh review found no scoped high | a new physical Location registration rejects callbacks before durable acceptance and fixes before its immutable provider/authorization floor. Compatible authorization refresh can only raise that floor; every retry reapplies floor, freshness, future-time, and cutoff qualification. Durable/duplicate ordinal high-water is monotonic within one physical run and resets only for a new run. This changes no canonical writer or rollout state. Evidence is host/Robolectric only, not real provider, passive/active cadence, process/reboot/FGS, battery/OEM, product query/UI, activation, shadow parity, cutover, push, or release proof. |
| TI-B186 | Activity freshness-envelope commit `3c7b28545`; focused receiver/retry/factory/Room ingress gate; Detekt and Activity lint; fresh independent corrected-diff review; local fast-forward | serialized focused command `BUILD SUCCESSFUL in 4m 4s` (269 tasks), XML `112/112` with zero failures/errors/skips; Detekt plus `:sensor:activity:lintDebug` `BUILD SUCCESSFUL in 1m 22s` (348 tasks), no new issue; fresh review found no scoped blocker/high/medium | invalid, overflowed, pre-acceptance, cutoff-invalid, and future provider times cannot fabricate Activity evidence or suppress valid siblings. Original indexes survive sparse qualification, and durable selection alone gives recognition precedence or authorizes transition-derived Activity fallback while the transition stream remains independent. This is host/Robolectric/static evidence, not real PendingIntent/provider, process/reboot, automation, FGS, battery/OEM, product fact/query/UI, deletion/export, activation, rollout, push, or release proof. |

Known historical failure notes in `.github/context/FOLLOWUPS.md` are not treated as current baseline results until reproduced.
