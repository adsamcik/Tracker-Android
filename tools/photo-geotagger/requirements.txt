# Tracker Photo Geotagger - Python Dependencies

# ExifTool wrapper (primary metadata engine)
PyExifTool>=0.5.6

# Track parsing
gpxpy>=1.6.2         # GPX format parser
lxml>=5.1.0          # KML XML parsing

# CLI framework
click>=8.1.7         # Command-line interface
colorama>=0.4.6      # Cross-platform colored terminal output

# Utilities
python-dateutil>=2.8.2  # Timezone handling, ISO 8601 parsing
tqdm>=4.66.1         # Progress bars
tabulate>=0.9.0      # Report table formatting

# Image handling (required for tests)
pillow>=10.2.0       # Image creation in tests

# Optional: HEIC support (if ExifTool not available)
# pillow-heif>=0.15.0

# Development dependencies (uncomment for development)
# pytest>=7.4.3
# pytest-cov>=4.1.0
# black>=24.1.0
# mypy>=1.8.0
